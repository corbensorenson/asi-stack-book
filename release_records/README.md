# Living Book Release Records

This directory contains public-safe records for rendered, validated living-book releases.

Each `*.json` file is validated by:

```bash
python3 scripts/validate_protocol_examples.py
```

The validation checks record shape against `schemas/living_book_release_record.schema.json`. It does not prove manuscript quality, claim truth, source interpretation, benchmark validity, or finality.
