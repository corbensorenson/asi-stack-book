# Claim Dossier: Context Transactions, Snapshots, Mounts, and Taint

Chapter ID: `context-transactions-snapshots-mounts-and-taint`

Status: P1 semantic review pending

Core support state: `argument`

This dossier is generated from the manifest and review overlay. Inclusion is not proof or promotion.

| Atom | Role | Type | Review | Proposition |
|---|---|---|---|---|
| `context-transactions-snapshots-mounts-and-taint.core` | `core` | `composite` | `semantically_reviewed` | Context Transactions should own the dynamic, versioned state-transition contract for durable context memory. Each accepted transaction binds principal, consumer, purpose, operation, base snapshot, branch, mounts, actual read/write/derive/delete/revoke sets, isolation and conflict policy, authority and rights, taint and declassification, durability and recovery model, budget, horizon, and support ceiling to an observed pre-state and a causally ordered attempted, applied, durable, visible, replayed, or recovered post-state. Commit, branch, merge, abort, retry, compaction, deletion, revocation, and recovery must preserve exact identities, obligations, faults, costs, and residuals. The transaction layer may change durable context state, but it does not own static packet materialization, semantic truth, belief revision, model/optimizer state, external effects, artifact correctness, verification adequacy, support, or release. |
| `context-transactions-snapshots-mounts-and-taint.problem.001` | `problem` | `source-synthesis` | `semantically_reviewed` | Long-lived agents and parallel workers mutate durable context through multiple stores, branches, indexes, caches, summaries, and recovery paths. Without an exact transaction boundary, the stack cannot tell which pre-state was read, which writes and derivatives actually occurred, which conflicts or partial failures happened, which state became durable and visible, or whether taint, revocation, deletion, authority, and rights obligations survived concurrency, retry, compaction, crash, and restart. |
| `context-transactions-snapshots-mounts-and-taint.insufficiency.001` | `insufficiency` | `composite` | `semantically_reviewed` | Vector retrieval, GraphRAG, and persistent conversational memory can improve access, consolidation, or task performance, but do not alone supply transaction isolation, atomic cross-representation commit, crash durability, or derivative closure. Database and filesystem transactions supply stronger consistency and recovery semantics within declared byte, record, API, journal, and platform scopes, but do not by themselves define semantic-object identity, purpose-bound mounts, taint, authority, rights, support boundaries, or the storage-versus-model-erasure distinction. CAP-style limits, machine-unlearning work, and ordinary cache invalidation make those boundaries sharper rather than disappearing. |
| `context-transactions-snapshots-mounts-and-taint.minimum.001` | `minimum` | `executable` | `semantically_reviewed` | The smallest honest implementation boundary for Context Transactions, Snapshots, Mounts, and Taint is: The current minimum is a record-and-route scaffold: one public transaction schema and fixture; three valid and six expected-invalid hand-authored memory-store records; two valid and four expected-invalid ordered sequence records; one hand-authored five-project restart lifecycle and ten rejecting mutations; and twenty-three Lean theorem declarations grouped under four public targets. It checks finite committed-read visibility, branch and mount faults, taint/declassification, deletion/materialization blocks, replay and non-promotion boundaries, no-loss lifecycle identity, atomic index/content epochs, and exact authored restart visibility. It does not run concurrent actors, a deployed durable store, real copy-on-write merge, actual process crash, filesystem journal recovery, distributed commit, model-backed context use, storage-wide deletion closure, useful advantage, or transfer. The next honest minimum must execute prospectively declared stateful tasks through ordinary file or key-value updates, a strong transactional database or filesystem baseline, persistent-memory and retrieval baselines, and the governed context transaction route on at least two actual backends; inject concurrency anomalies, mount and taint violations, partial commits, cache/replica lag, process kills, restart, deletion and revocation, compaction, retries, and partitions; independently observe actual pre-state, writes, durability, visibility, recovery, leakage, task outcomes, costs, and delayed residuals; reproduce from locks; and transfer to a second model and backend. |
| `context-transactions-snapshots-mounts-and-taint.beyond_sota.001` | `beyond_sota` | `composite` | `semantically_reviewed` | A mature Context Transaction layer is an independently testable dynamic state machine for durable AI context, not a transactional metaphor. Natural stateful workloads run parallel readers and writers over exact source objects, branches, mounts, indexes, caches, derivatives, and recovery boundaries. Full attempt ledgers compare stateless and direct retrieval, GraphRAG, persistent memory, ordinary file and key-value updates, database or filesystem transactions, event-sourced designs, human-managed state, and the governed semantic transaction layer under matched models, sources, authority, resources, faults, and horizons. Independent observers measure isolation anomalies, source and field survival, conflict calibration, authority and rights leakage, taint and deletion closure, crash durability, restart fidelity, idempotent recovery, useful task throughput, unsafe use, abstention and missed help, privacy exposure, latency, and total cost. Promotion requires nonzero useful opportunity, predicted mechanism-specific failures under ablation, clean crash/restart reproduction, complete denominators, storage-versus-model-erasure honesty, and cross-model and cross-backend transfer; otherwise exact claims narrow, remain null or negative, become refuted, or are blocked after a full attempt. |
| `context-transactions-snapshots-mounts-and-taint.mechanism.001` | `mechanism` | `composite` | `semantically_reviewed` | Freeze principal, consumer, purpose, operation, durable object and store scope, branch and base snapshot, requested read/write/derive/delete/revoke sets, mount namespace, authority and rights ceiling, isolation, conflict and merge policy, durability and recovery model, budget, horizon, support ceiling, and material-change triggers before admission. |
| `context-transactions-snapshots-mounts-and-taint.mechanism.002` | `mechanism` | `composite` | `semantically_reviewed` | Assign stable transaction, event, object, version, field, branch, snapshot, mount, derivative, deletion-obligation, and receipt identities with causal parents, monotonic sequence or logical-clock evidence, and explicit clock uncertainty. |
| `context-transactions-snapshots-mounts-and-taint.mechanism.003` | `mechanism` | `composite` | `semantically_reviewed` | Capture an observed pre-state binding exact source versions, content and index epochs, provenance and taint state, mount policy, locks or leases, caches, descendants, backups, and open obligations before mutation. |
| `context-transactions-snapshots-mounts-and-taint.mechanism.004` | `mechanism` | `composite` | `semantically_reviewed` | Enforce purpose-bound mount namespaces with separate read, write, derive, delete, share, export, train, and execute permissions; transaction admission cannot widen authority or rights. |
| `context-transactions-snapshots-mounts-and-taint.mechanism.005` | `mechanism` | `composite` | `semantically_reviewed` | Record actual reads, writes, derivations, deletions, revocations, cache touches, and side records rather than trusting requested or declared sets. |
| `context-transactions-snapshots-mounts-and-taint.mechanism.006` | `mechanism` | `composite` | `semantically_reviewed` | Declare and test the isolation contract, including dirty reads, non-repeatable reads, phantoms, lost updates, write skew, stale-index reads, cross-branch leakage, and unauthorized mount visibility. |
| `context-transactions-snapshots-mounts-and-taint.mechanism.007` | `mechanism` | `composite` | `semantically_reviewed` | Give copy-on-write branches explicit create, rebase, compare, merge, reject, quarantine, abort, and garbage-collection states; conflicts and unresolved obligations cannot collapse into silent last-writer-wins. |
| `context-transactions-snapshots-mounts-and-taint.mechanism.008` | `mechanism` | `composite` | `semantically_reviewed` | Separate requested, admitted, prepared, applied, durably committed, consumer-visible, replayed, recovered, compensated, aborted, and indeterminate states with typed faults and observed transition receipts. |
| `context-transactions-snapshots-mounts-and-taint.mechanism.009` | `mechanism` | `composite` | `semantically_reviewed` | Commit content, indexes, provenance, taint, authority and rights, derivative edges, deletion and revocation obligations, caches, and audit refs atomically within the declared boundary or expose every partial commit and uncertain participant as a blocking residual. |
| `context-transactions-snapshots-mounts-and-taint.mechanism.010` | `mechanism` | `composite` | `semantically_reviewed` | Bind retries to idempotency keys, original transaction and attempt lineage, effect observations, and duplicate suppression; a timeout, missing acknowledgement, or replay cannot silently repeat a non-idempotent write. |
| `context-transactions-snapshots-mounts-and-taint.mechanism.011` | `mechanism` | `composite` | `semantically_reviewed` | Propagate provenance, taint, privacy, authority, rights, purpose, retention, and prohibited-use labels through every derivative unless a separately authorized and evidenced declassification narrows them. |
| `context-transactions-snapshots-mounts-and-taint.mechanism.012` | `mechanism` | `composite` | `semantically_reviewed` | Track deletion and revocation closure across live objects, derivatives, summaries, embeddings, indexes, caches, replicas, exports, backups, and future materializations, while reporting inaccessible, logically deleted, physically erased, behaviorally removed, influence-removed, privacy-protected, and unknown states separately. |
| `context-transactions-snapshots-mounts-and-taint.mechanism.013` | `mechanism` | `composite` | `semantically_reviewed` | Record contradiction, correction, supersession, and invalidation as append-only context events without letting the transaction layer decide claim truth or belief support. |
| `context-transactions-snapshots-mounts-and-taint.mechanism.014` | `mechanism` | `composite` | `semantically_reviewed` | Bind freeze, thaw, rebalance, tier promotion, no-loss compaction, migration, and restoration to observed preconditions, postconditions, item identities, representation loss, costs, acknowledgements, and rollback or residual state. |
| `context-transactions-snapshots-mounts-and-taint.mechanism.015` | `mechanism` | `composite` | `semantically_reviewed` | Inject partial writes, process termination, journal loss, index/content divergence, cache staleness, lease expiry, and restart; recover only to a declared durable boundary and compare exact visible state and open obligations after restart. |
| `context-transactions-snapshots-mounts-and-taint.mechanism.016` | `mechanism` | `composite` | `semantically_reviewed` | Observe actual durable state and consumer visibility independently of transaction logs, success returns, checksums, acknowledgements, and replay claims. |
| `context-transactions-snapshots-mounts-and-taint.mechanism.017` | `mechanism` | `composite` | `semantically_reviewed` | Preserve every admitted, rejected, conflicted, retried, timed-out, partially applied, recovered, compensated, aborted, discarded, human-repaired, and residual attempt plus total storage, compute, latency, privacy, recovery, and opportunity cost. |
| `context-transactions-snapshots-mounts-and-taint.mechanism.018` | `mechanism` | `composite` | `semantically_reviewed` | Compare the governed transaction layer with stateless retrieval, GraphRAG, persistent memory, ordinary file and key-value updates, database or filesystem transactions, event-sourced designs, and human-managed state under matched sources, models, authority, resources, faults, and horizons. |
| `context-transactions-snapshots-mounts-and-taint.interface.001` | `interface` | `executable` | `semantically_reviewed` | The Virtual Context ABI consumes one exact committed or explicitly faulted snapshot and materializes a static packet; it cannot mutate, merge, delete, revoke, or recover durable context. |
| `context-transactions-snapshots-mounts-and-taint.interface.002` | `interface` | `executable` | `semantically_reviewed` | Intent, Cognitive Compilation, and Planning may request context-state operations and consistency needs but cannot declare a write applied, durable, visible, recovered, truthful, useful, or supported. |
| `context-transactions-snapshots-mounts-and-taint.interface.003` | `interface` | `executable` | `semantically_reviewed` | Durable context stores expose observed object, version, index, journal, cache, replica, backup, and restart state; the transaction contract coordinates changes only within its declared participants and failure model. |
| `context-transactions-snapshots-mounts-and-taint.interface.004` | `interface` | `executable` | `semantically_reviewed` | Security, Privacy, Authority, Rights, and Licensing owners decide mount grants, write/delete/export/train permissions, declassification, retention, disclosure, and prohibited uses. |
| `context-transactions-snapshots-mounts-and-taint.interface.005` | `interface` | `executable` | `semantically_reviewed` | Claim Ledgers and Spinoza own contradiction, evidence, belief, and support transitions; a context correction, supersession, deletion, or commit cannot promote or refute a claim by itself. |
| `context-transactions-snapshots-mounts-and-taint.interface.006` | `interface` | `executable` | `semantically_reviewed` | Artifact Graphs preserve transaction, object, derivative, cache, backup, fault, recovery, and downstream lineage without certifying transactional correctness. |
| `context-transactions-snapshots-mounts-and-taint.interface.007` | `interface` | `executable` | `semantically_reviewed` | Runtime Adapters own jobs, tools, approvals, and external effects; context-state abort, restore, or compensation cannot claim rollback of an external effect without separate effect-complete evidence. |
| `context-transactions-snapshots-mounts-and-taint.interface.008` | `interface` | `executable` | `semantically_reviewed` | Data Engines own datasets, model weights, optimizer and scheduler state, training caches, checkpoints, descendants, and causal unlearning; storage deletion is not model forgetting, influence removal, privacy erasure, or behavioral cohort removal. |
| `context-transactions-snapshots-mounts-and-taint.interface.009` | `interface` | `executable` | `semantically_reviewed` | Verification Bandwidth owns independent adequacy and outcome evaluation; transaction validity establishes only the declared state-transition properties. |
| `context-transactions-snapshots-mounts-and-taint.interface.010` | `interface` | `executable` | `semantically_reviewed` | Resource Economics owns observed storage, compute, indexing, locking, validation, retry, human, recovery, privacy, latency, and opportunity cost. |
| `context-transactions-snapshots-mounts-and-taint.interface.011` | `interface` | `executable` | `semantically_reviewed` | Evidence States and Readiness Gates own support, quarantine, promotion, and release; a green commit, replay, deletion receipt, or restart check has no direct support-state effect. |
| `context-transactions-snapshots-mounts-and-taint.invariant.001` | `invariant` | `formal` | `semantically_reviewed` | Every transaction binds one exact principal, consumer, purpose, operation, participant set, branch, base snapshot, mount policy, authority and rights ceiling, isolation, durability, recovery model, budget, horizon, and support ceiling. |
| `context-transactions-snapshots-mounts-and-taint.invariant.002` | `invariant` | `formal` | `semantically_reviewed` | Requested, admitted, prepared, applied, durable, visible, replayed, recovered, compensated, aborted, and indeterminate states remain distinct and require their own observations. |
| `context-transactions-snapshots-mounts-and-taint.invariant.003` | `invariant` | `formal` | `semantically_reviewed` | A snapshot binds complete declared causal, object-version, branch, mount, content/index epoch, policy, provenance, taint, lease, cache, and open-obligation boundaries; a name or timestamp alone is not a snapshot. |
| `context-transactions-snapshots-mounts-and-taint.invariant.004` | `invariant` | `formal` | `semantically_reviewed` | Actual read, write, derive, delete, revoke, cache, replica, backup, and export sets remain inspectable; requested or declared sets cannot substitute for observed effects. |
| `context-transactions-snapshots-mounts-and-taint.invariant.005` | `invariant` | `formal` | `semantically_reviewed` | No uncommitted, stale, foreign-branch, policy-mismatched, or unauthorized state becomes visible where the declared isolation and mount contract forbid it. |
| `context-transactions-snapshots-mounts-and-taint.invariant.006` | `invariant` | `formal` | `semantically_reviewed` | Concurrent conflicts, lost updates, write skew, phantoms, stale indexes, and merge disagreements remain explicit; silent last-writer-wins cannot masquerade as a conflict-free commit. |
| `context-transactions-snapshots-mounts-and-taint.invariant.007` | `invariant` | `formal` | `semantically_reviewed` | Mounts, branches, summaries, embeddings, translations, caches, replicas, and exports cannot widen authority, rights, purpose, retention, permitted use, support, or release. |
| `context-transactions-snapshots-mounts-and-taint.invariant.008` | `invariant` | `formal` | `semantically_reviewed` | Provenance, taint, privacy, authority, rights, prohibited use, and unresolved conflict propagate to every derivative unless a separately authorized declassification records evidence, scope, consequence, expiry, and residual. |
| `context-transactions-snapshots-mounts-and-taint.invariant.009` | `invariant` | `formal` | `semantically_reviewed` | Deletion and revocation closure states name exact reachable storage and derivative scope; logical deletion, inaccessibility, physical erasure, behavioral removal, influence removal, privacy protection, and unknown completion are never conflated. |
| `context-transactions-snapshots-mounts-and-taint.invariant.010` | `invariant` | `formal` | `semantically_reviewed` | Atomicity, consistency, isolation, and durability claims are limited to the declared participants, operations, journal, platform, fault model, and observation boundary; uncertain or partitioned participants remain residual rather than silently committed. |
| `context-transactions-snapshots-mounts-and-taint.invariant.011` | `invariant` | `formal` | `semantically_reviewed` | Durable means the declared state survives the declared crash and restart model and is independently observed after recovery; a success return, log row, checksum, or acknowledgement is insufficient. |
| `context-transactions-snapshots-mounts-and-taint.invariant.012` | `invariant` | `formal` | `semantically_reviewed` | Freeze, thaw, rebalance, promotion, compaction, migration, restore, and every no-loss transition preserve each authoritative identity and obligation or expose exact loss, corruption, conflict, and repair state. |
| `context-transactions-snapshots-mounts-and-taint.invariant.013` | `invariant` | `formal` | `semantically_reviewed` | Retries and replay preserve original transaction identity, attempt lineage, idempotency policy, duplicate detection, and observed effects; non-idempotent writes cannot silently execute twice. |
| `context-transactions-snapshots-mounts-and-taint.invariant.014` | `invariant` | `formal` | `semantically_reviewed` | Context rollback, abort, restore, or compensation cannot erase an already observed external effect, published artifact, human disclosure, model update, or downstream copy without separate owner evidence. |
| `context-transactions-snapshots-mounts-and-taint.invariant.015` | `invariant` | `formal` | `semantically_reviewed` | Actual pre-state, post-state, durability, consumer visibility, restart visibility, and downstream consequence remain independently observable; transaction theater cannot substitute declarations for effects. |
| `context-transactions-snapshots-mounts-and-taint.invariant.016` | `invariant` | `formal` | `semantically_reviewed` | Every conflict, denial, retry, timeout, partial write, crash, recovery, human repair, abort, discard, cost, and residual remains in the denominator. |
| `context-transactions-snapshots-mounts-and-taint.invariant.017` | `invariant` | `formal` | `semantically_reviewed` | Schemas, hand-authored fixtures, finite event records, local reversible traces, and finite theorems establish only their exact scopes and cannot universalize isolation, durability, deletion, security, usefulness, or transfer. |
| `context-transactions-snapshots-mounts-and-taint.failure_mode.001` | `failure_mode` | `causal` | `semantically_reviewed` | For Context Transactions, Snapshots, Mounts, and Taint, a material failure mode is: Transaction theater records requested or acknowledged state changes without observing actual pre-state, durable post-state, visibility, consequence, or recovery. |
| `context-transactions-snapshots-mounts-and-taint.failure_mode.002` | `failure_mode` | `causal` | `semantically_reviewed` | For Context Transactions, Snapshots, Mounts, and Taint, a material failure mode is: Snapshot fiction mixes versions, branches, mounts, policies, caches, or causal frontiers while presenting one coherent-view identity. |
| `context-transactions-snapshots-mounts-and-taint.failure_mode.003` | `failure_mode` | `causal` | `semantically_reviewed` | For Context Transactions, Snapshots, Mounts, and Taint, a material failure mode is: Isolation laundering calls a local sequential fixture evidence for concurrent serializability, read-your-writes, or cross-store consistency. |
| `context-transactions-snapshots-mounts-and-taint.failure_mode.004` | `failure_mode` | `causal` | `semantically_reviewed` | For Context Transactions, Snapshots, Mounts, and Taint, a material failure mode is: Lost-update and write-skew laundering silently accepts overlapping decisions because record-level conflicts or semantic invariants were not modeled. |
| `context-transactions-snapshots-mounts-and-taint.failure_mode.005` | `failure_mode` | `causal` | `semantically_reviewed` | For Context Transactions, Snapshots, Mounts, and Taint, a material failure mode is: Branch and merge laundering leaks speculative state, resolves conflicts through last-writer-wins, or garbage-collects an unmerged obligation. |
| `context-transactions-snapshots-mounts-and-taint.failure_mode.006` | `failure_mode` | `causal` | `semantically_reviewed` | For Context Transactions, Snapshots, Mounts, and Taint, a material failure mode is: Mount-authority laundering exposes or mutates context through a path, cache, replica, summary, tool, or restore route outside the purpose-bound grant. |
| `context-transactions-snapshots-mounts-and-taint.failure_mode.007` | `failure_mode` | `causal` | `semantically_reviewed` | For Context Transactions, Snapshots, Mounts, and Taint, a material failure mode is: Taint and declassification laundering drops provenance or restrictions through derivation, aggregation, translation, compaction, migration, or a success-shaped approval record. |
| `context-transactions-snapshots-mounts-and-taint.failure_mode.008` | `failure_mode` | `causal` | `semantically_reviewed` | For Context Transactions, Snapshots, Mounts, and Taint, a material failure mode is: Deletion resurrection returns revoked or deleted material through derivatives, embeddings, indexes, caches, backups, exports, restored snapshots, or future materializations. |
| `context-transactions-snapshots-mounts-and-taint.failure_mode.009` | `failure_mode` | `causal` | `semantically_reviewed` | For Context Transactions, Snapshots, Mounts, and Taint, a material failure mode is: Erasure-category laundering treats logical deletion, access denial, storage erasure, model forgetting, influence removal, privacy protection, or changed behavior as interchangeable. |
| `context-transactions-snapshots-mounts-and-taint.failure_mode.010` | `failure_mode` | `causal` | `semantically_reviewed` | For Context Transactions, Snapshots, Mounts, and Taint, a material failure mode is: Partial-write laundering admits new content with an old index, new index with old content, or divergent provenance, taint, rights, and deletion ledgers. |
| `context-transactions-snapshots-mounts-and-taint.failure_mode.011` | `failure_mode` | `causal` | `semantically_reviewed` | For Context Transactions, Snapshots, Mounts, and Taint, a material failure mode is: Crash and restart amnesia loses committed identities, obligations, locks, leases, journal position, cache invalidations, or recovery residuals while reporting healthy state. |
| `context-transactions-snapshots-mounts-and-taint.failure_mode.012` | `failure_mode` | `causal` | `semantically_reviewed` | For Context Transactions, Snapshots, Mounts, and Taint, a material failure mode is: Retry and acknowledgement laundering repeats a non-idempotent write after timeout or reuses a success receipt for an unobserved attempt. |
| `context-transactions-snapshots-mounts-and-taint.failure_mode.013` | `failure_mode` | `causal` | `semantically_reviewed` | For Context Transactions, Snapshots, Mounts, and Taint, a material failure mode is: Causal-order laundering uses wall-clock order, stale logical clocks, or missing parents to present a reordered history as valid. |
| `context-transactions-snapshots-mounts-and-taint.failure_mode.014` | `failure_mode` | `causal` | `semantically_reviewed` | For Context Transactions, Snapshots, Mounts, and Taint, a material failure mode is: Cache, replica, and backup laundering leaves a hidden participant outside invalidation, taint, rights, revocation, deletion, or recovery closure. |
| `context-transactions-snapshots-mounts-and-taint.failure_mode.015` | `failure_mode` | `causal` | `semantically_reviewed` | For Context Transactions, Snapshots, Mounts, and Taint, a material failure mode is: Compaction and tiering loss removes low-salience but authoritative, contradictory, minority, legal, evidential, or deletion-critical state under a no-loss label. |
| `context-transactions-snapshots-mounts-and-taint.failure_mode.016` | `failure_mode` | `causal` | `semantically_reviewed` | For Context Transactions, Snapshots, Mounts, and Taint, a material failure mode is: Rollback and compensation laundering calls a context restore effect-complete rollback while external effects, artifacts, disclosures, model updates, or descendants persist. |
| `context-transactions-snapshots-mounts-and-taint.failure_mode.017` | `failure_mode` | `causal` | `semantically_reviewed` | For Context Transactions, Snapshots, Mounts, and Taint, a material failure mode is: Failure and cost survivorship removes rejected, conflicted, retried, crashed, repaired, aborted, slow, privacy-expensive, or residual transactions from estimates. |
| `context-transactions-snapshots-mounts-and-taint.failure_mode.018` | `failure_mode` | `causal` | `semantically_reviewed` | For Context Transactions, Snapshots, Mounts, and Taint, a material failure mode is: Portability theater treats one authored fixture, storage backend, filesystem, model, workload, or fault schedule as evidence of general transaction semantics. |
| `context-transactions-snapshots-mounts-and-taint.formal.vcm-transactions-operational-invariant` | `formal_target` | `formal` | `semantically_reviewed` | Accepted reads preserve exact snapshot, branch, mount, cell, committed version, and replay custody before materialization. |
| `context-transactions-snapshots-mounts-and-taint.formal.vcm-transactions-failure-blocks-promotion` | `formal_target` | `formal` | `semantically_reviewed` | Taint removal requires represented declassification authority and receipt; open deletion or support promotion without its receipt blocks materialization. |
| `context-transactions-snapshots-mounts-and-taint.formal.vcm-transactions-memory-store-fixture-bridge` | `formal_target` | `formal` | `semantically_reviewed` | The reachable model is consumed alongside the exact 3-valid/6-invalid memory-store suite and 78-mutation independent checker. |
| `context-transactions-snapshots-mounts-and-taint.formal.vcm-transactions-sequence-fixture-bridge` | `formal_target` | `formal` | `semantically_reviewed` | The six-event ordered witness is consumed alongside the exact 2-valid/4-invalid sequence suite and rejects read-before-write and missing-replay faults. |

## Argument-exit state

No promotion-or-refutation campaign is frozen yet. P1 must first replace every machine candidate with a semantic review and adjudicate every prose-only candidate.

## Non-claims

- This dossier does not establish semantic adequacy, implementation behavior, empirical benefit, transfer, safety, or SOTA status.
- All support movement requires a separate accepted evidence-transition record.
