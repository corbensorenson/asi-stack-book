# Model adequacy dossier: compact generation refinement

## Modeled boundary

The model covers nine reachable lifecycle stages and exact identity binding for representation, source, contract, generator, target, verifier, residual ledger, consumer, result set, and event. Route priority covers malformed or substituted records, rights and source binding, generation inputs and bounds, observed reconstruction, verifier identity and independence, lossy exactness, executable preserved-source fallback, residual ownership and burden, result-digest binding, evidence transitions, semantic provenance and migration, consumer compatibility and evaluation, residual closure, replay, and authority leaks.

## Consumers and mutations

The independent Python consumer recompiles the exact 35-theorem surface, executes one complete eight-event fallback lifecycle, verifies all nine prefix/suffix composition splits, preserves all ten identity fields and zero support/effect authority at every reachable state, checks fallback monotonicity across all eight transitions, reaches all 60 Lean route constructors, rejects all eight event kinds after closure, and rejects 163/163 lifecycle mutations. It also reads and SHA-256 binds the four existing bounded result families rather than copying their Boolean summaries into Lean.

## Honest exclusions

The model does not implement a codec, generator, verifier, provenance service, semantic graph, residual detector, consumer, or deployed ledger. Authored Boolean fields are not measurements. Route reachability does not establish empirical adequacy. The source results remain synthetic/local fixtures, and their digests establish identity only. No support transition, safety result, compression utility, semantic utility, reproduction, transfer, SOTA, AGI, or ASI result follows.
