# P5 Mandatory Three-Atom Terminal Batch

## Outcome

The batch was frozen before its terminal audit with exactly the three atoms
named by the roadmap, atom-specific falsifiers, bounded claim ceilings, a
single-shot evaluator, and no outcome-aware retry. The evaluator independently
replayed the retained Circle, Authority Effect, Capability Replacement, and M7
update/unlearning artifacts rather than treating their existing green labels as
proof.

| Atom | Terminal disposition | Exact boundary |
|---|---|---|
| `circle-calculus-and-proof-carrying-ai-contracts.mechanism.003` | `promoted_at_bounded_scope` to `prototype-backed` | One pinned Circle rope target compiled; command, environment/result receipts, proof boundary, and four rejecting consumer controls are retained. No general proof transport, semantic refinement, multi-consumer benefit, or transfer follows. |
| `system-boundaries-and-authority.invariant.001` | `promoted_at_bounded_scope` to `synthetic-test-backed` | The finite six-event grant-to-effect model retained two pre-effect denials, zero governed unsafe releases, and 38/38 rejected authority mutations. Identities, receipts, clocks, and enforcement remain trusted local inputs. |
| `capability-replacement-and-rollback.invariant.011` | `narrowed_after_full_attempt`; remains `argument` | Artifact/digest restoration, behavior, membership-attack privacy proxy, storage, backups, and descendants remained separately reported. Service restart and external compensation were not executed, so the complete six-outcome proposition did not earn promotion. |

The terminal result is
`experiments/p5_terminal_batch/results/result.json`, validated by
`python3 scripts/validate_p5_terminal_batch.py`. Two subordinate atoms move at
bounded scope, one is narrowed after a competent full attempt, and zero chapter
core claims move. The batch does not establish deployed proof transport,
authorization enforcement, distributed revocation, open-world effect
completeness, production rollback, privacy repair, external compensation,
transfer, SOTA, AGI, ASI, publication authority, or release authority.
