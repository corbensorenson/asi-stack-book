# Governed Deliberation request-to-closure refinement

The replacement model has eight reachable stages: request, scope binding, candidate generation, evaluation, selection, stopping, bounded planning handoff, and closure. It exposes 59 routes and separates request, policy, budget, evaluator, result, residual, event, first-candidate, candidate-history, corruption, repair, faithfulness, cost, stop, handoff, support, and external-effect state.

`python3 scripts/validate_deliberation_refinement.py` independently reaches all 59 routes, rejects 51/51 non-accepting mutations, recompiles Lean, reruns the exact ten-case deliberation-admission harness, revalidates the three-seed post-v2 campaign with 900 routing, 540 deliberation, and 180 interference records, and revalidates the later actual-model outcome ledger. That actual-model five-arm 0/60 result contains 60 Qwen3-4B-4bit requests, 240 model calls, 360 candidate evaluations, five arms, 1,140 candidate operations, 0/60 final correct in every arm, no initially correct cases, a `no_change` deliberation disposition, and `no_core_promotion`. The consumer preserves all fifteen fixed-step harms and zero synthetic fallback activations and SHA-256 binds all four source artifacts.

The verified witness reaches closure with one candidate batch, evaluation, selection, bounded planning handoff, and no support or external effect. A separate no-verified-candidate witness reaches closure through residual escrow. These are finite local lifecycle facts plus an exact failed actual-model attempt, not evidence of useful language-model reasoning, natural-workload utility, verifier independence or correctness, trace faithfulness, useful throughput, calibrated abstention, deployed fallback, planning execution, reproduction, transfer, or state of the art. Support-state and external-effect authority remain exactly `none`.
