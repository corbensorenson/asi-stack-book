# P7.2-T2 Governed World Models Reader Integration

Date: 2026-07-19

Packet: `P7.2-T2-governed-world-models-and-reality-grounding`

Machine authority: `evidence_quality/p7_2_t2_governed_world_models_reader_integration.json`

## Terminal decision

Governed World Models and Reality Grounding is terminally integrated as a
load-bearing reference chapter at `argument` support. It owns a distinct object
and decision boundary: the versioned, non-authorizing predictive-state packet
that may influence a plan, and the residual that reconciles that imagined
branch with a later independently admitted observation. Planning owns goals and
route choice, Perception owns observation admission, Runtime owns effect
authority, and Artifact Graphs own general provenance; none owns this
prediction-to-observation boundary.

## What is implemented

- `schemas/governed_world_model_packet.schema.json` binds exact model,
  observation, belief, rollout, prediction, calibration, disagreement, planner,
  authority, reconciliation, residual, lifecycle, and non-claim custody.
- The valid fixture is deliberately stale, unsupported, materially disagreeing,
  and routed to safe hold. It is a record-shape fixture, not a predictor result.
- `AsiStackProofs.GovernedWorldModels` implements both planned public targets
  through nine theorem declarations. It can admit a branch for bounded planning
  or require rejection, re-observation, fallback, review, re-estimation, or safe
  hold, but it has no effect-authority constructor reachable from the router.
- `scripts/validate_governed_world_model_contract.py` checks the schema, fixture,
  protocol, Lean surface, chapter ceiling, and thirteen rejecting mutations.

## Evidence separation

The earlier P4/M8 campaign exercised two finite authored environments, 11,250
episodes, 6,000 held-out episodes, and ten governed replacement/rollback cases.
It remains adjacent bounded synthetic evidence. It did not train a neural world
model, use natural partially observed tasks, establish independent real-world
truth, test planner exploitation adequately, reproduce, or transfer; therefore
it contributes no chapter-core support.

The claim-bearing six-arm protocol is ready but not executed. It freezes eight
competence gates, seven rescue steps, ten joint outcomes, six causal ablations,
an N3 exact ceiling, and P2 resource/denominator isolation. Reactive,
model-free, ordinary predictive-control, single-governed, plural-governed, and
oracle-sensitivity arms must be fairly matched. A cheap toy run would not answer
the chapter claim and cannot be used to infer architectural failure.

## Reader reconciliation

The overview map, Planning handoff, Cognitive Compilation intake, glossary,
claim matrix, source appendices, and Integrated Reference Architecture now
share the qualified-branch and reality-residual vocabulary. All nine sources
remain design inputs or external comparators; none is inherited as local
grounding, model-quality, control, or safety evidence.

Support, release, and publication effects remain `none`. The next first-tranche
owner is Human Factors and Meaningful Control in Oversight.
