# Adversarial-evaluation observation-lifecycle refinement

**State:** finite authored lifecycle validated; no chapter-core support change.
**Receipt:** `experiments/adversarial_evaluation_refinement/results/2026-07-15-local.json`

The refinement replaces isolated integrity-route reductions with a reachable seven-stage lifecycle: draft, scoped, protocol-bound, observed, independently probed, adjudicated, and decision-bound. Exact consumer, decision, model, task, protocol, policy, evaluator, monitor, reward, selection, hypothesis, outcome, and protocol-version custody persists across every accepted transition.

The independently implemented consumer preserves the original eight-case integrity suite, reaches all 56 routes, and rejects 60/60 identity, context, provenance, denominator, independent-evaluator, cross-context, alternative-hypothesis, discrepancy, mitigation, quarantine, decision-handoff, intent-inference, authority, replay, expiry, and re-evaluation mutations. The full witness emits one bounded decision-review handoff and then requires a recorded trigger, complete descendant invalidation, ordinary-route blocking, and protocol version 2 before returning to scoped protocol binding. It emits no support assignment or external effect.

This is observation-record discipline. It does not detect deception, sandbagging, alignment faking, reward hacking, or evaluator compromise; establish model capability, intent, prevalence, monitor/reward fidelity, evaluator independence, outcome validity, mitigation efficacy, quarantine correctness, readiness, safety, release, or transfer; or authorize any external decision. Support-state effect is exactly `none`.
