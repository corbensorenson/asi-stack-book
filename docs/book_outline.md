# Cohesive Book Outline

Working title: **The ASI Stack: A Systems Architecture for Governed, Efficient, Self-Improving AI**

Status: architecture outline for drafting the full book. This document defines the intended argument arc, chapter jobs, and bibliography plan. `book_structure.json` remains the ordering source of truth; displayed chapter numbers are generated by Quarto.

## Drafting Thesis

The book argues that advanced AI should not be understood as one larger model, one agent loop, or one benchmark ladder. It should be specified as a governed stack whose layers transform intent into action through explicit boundaries: alignment, governance, planning, memory, reasoning, execution, routing, compression, evidence, and recursive self-improvement.

The stack is efficient because it does not spend maximal cognition everywhere. It routes work, compiles context, reuses artifacts, compresses representations, escalates only when needed, and records residual uncertainty. It is governable because each layer has an authority ceiling, an interface, an invariant set, a failure model, and an evidence record. It is self-improving only when replacement is bounded by stable capability fields, readiness gates, rollback paths, and evaluator integrity.

## Reader Promise

By the end of the book, a technical reader should understand:

- Why ASI should be treated as a systems architecture, not a monolithic model.
- How each layer constrains the others without erasing its separate responsibilities.
- How claims move from design argument to source-derived, prototype-backed, test-backed, or externally supported evidence states.
- How a minimal implementation can grow from artifact graph to governed capability replacement.
- How the living book itself acts as an evidence ratchet instead of a static anthology.

## Book-Level Throughline

1. Define the stack and why model scale alone is not an architecture.
2. Establish efficiency as governed selectivity: routing, compression, memory reuse, and benchmark pressure.
3. Name the failure modes that appear when intelligence is not bounded by authority, evidence, memory discipline, or evaluator integrity.
4. Specify each layer as a boundary with inputs, outputs, invariants, and tests.
5. Integrate the layers into an intent-to-execution reference architecture.
6. Convert the architecture into a staged prototype and living evidence process.

## Lean Proof Target Format

The outline is the source of truth for proof scope. Every chapter with a `Stable ID` must include a `Lean proof targets` table. These rows tell future drafting agents which claims should become executable Lean work, which module should contain them, and whether the target is only planned or already implemented.

Proof rows use this format:

| Field | Meaning |
|---|---|
| Tag | Stable proof identifier. Must start with `lean:` and remain stable across edits. |
| Lean module | Intended module under `lean/AsiStackProofs/`. |
| Formal target | The operational invariant, relation, transition rule, or schema property to formalize. |
| Status | `planned`, `scaffolded`, `implemented`, `blocked`, or `retired`. |

Rules:

- Do not tag broad philosophical claims as Lean targets until they are translated into operational predicates.
- Do not describe a target as proven unless the corresponding Lean module exists and `lake build` passes.
- Prefer small invariants over grand architecture theorems.
- If a chapter changes its mechanism, update its proof tags in this outline before drafting proof prose.

## Front Matter

### Landing Page

Purpose: give readers a current-state dashboard for the living book.

Must contain:

- Book thesis and architecture diagram.
- Source/evidence status.
- Warning that chapters are living drafts, not final claims.
- Links to matrices, schemas, tests, and changelog.

### Preface

Purpose: explain why the source papers are being synthesized into one architecture.

Drafting tasks:

- State the book's scope and limits.
- Explain support states.
- Explain how to read speculative material.
- Explain how the Quarto/GitHub Pages workflow keeps the book living.

## Part I - The Stack from Above

Part job: establish the architecture problem before any layer details. Part I should make the reader feel why a governed stack is necessary, what efficiency means, and what fails without explicit boundaries.

### ASI Is a Stack, Not a Model

Stable ID: `asi-is-a-stack-not-a-model`

Chapter job: introduce the master frame. The chapter should turn the source corpus from a set of separate papers into one systems architecture.

Core claim: efficient ASI should be modeled as a governed stack of cooperating layers rather than as a single undifferentiated model.

Draft arc:

- Define "stack" in this book: layered responsibilities, typed handoffs, authority ceilings, and evidence states.
- Contrast model scale, agent loops, and tool wrappers with a full architecture.
- Introduce the canonical layer map.
- Show the intent-to-execution path at high level.
- Introduce the artifact graph and claim ledger as book-wide connective tissue.
- Define what the book will not claim: guaranteed safety, solved alignment, or complete ASI.

Primary interfaces:

- Alignment constrains goals.
- Governance bounds authority and replacement.
- Planning converts goals into task graphs.
- VCM compiles context packets.
- Reasoning verifies claims.
- Execution produces artifacts.
- Routing allocates cognition.
- Evidence ratchets determine promotion.

Primary source spine: `viea`, `beastbrain`, `aletheia`, `moecot`, `talos`.

Draft deliverables:

- Master stack diagram.
- Layer-boundary table.
- Book vocabulary table for artifact, authority, evidence, route, residual, field, and ratchet.

Lean proof targets:

| Tag | Lean module | Formal target | Status |
|---|---|---|---|
| `lean:stack.layer_authority_separation` | `AsiStackProofs.StackBoundaries` | A layer without execution authority cannot produce an external action except through an authorized handoff. | planned |
| `lean:stack.handoff_preserves_authority_ceiling` | `AsiStackProofs.StackBoundaries` | Typed handoffs preserve or reduce the caller's authority ceiling. | planned |

### The Efficient ASI Hypothesis

Stable ID: `the-efficient-asi-hypothesis`

Chapter job: define efficiency as architectural selectivity, not cheapness alone.

Core claim: capability improves when cognition is routed, compressed, reused, governed, and tested.

Draft arc:

- Define efficiency across compute, context, verification effort, human attention, and risk.
- Explain minimum viable intelligence: use the smallest adequate route that preserves quality.
- Explain proceduralization: repeated cognition becomes durable artifacts, tools, or memory.
- Explain residual burden: compression and routing must reveal what they failed to solve.
- Explain fallback: lightweight routes need escalation paths.
- Tie efficiency to evidence: an efficiency gain is not real unless task quality and cost are both tracked.

Primary interfaces:

- Planning requests an adequate capability profile.
- Routing selects specialists and tools.
- Compression reduces representation burden while exposing residuals.
- Evidence decides whether efficiency claims survive tests.

Primary source spine: `viea`, `moecot`, `rmi`, `cgs`, `rankfold_neuralfold`, `bbvca_v9`, `simulation_scaling`, `beastbrain`, `beastbrain_timeless`, `aletheia`.

Draft deliverables:

- Efficiency ledger schema sketch.
- Minimum-viable-intelligence decision table.
- Residual-burden examples.

Lean proof targets:

| Tag | Lean module | Formal target | Status |
|---|---|---|---|
| `lean:efficiency.minimum_viable_route_order` | `AsiStackProofs.Efficiency` | A route is minimum viable only when no lower-cost authorized route satisfies the required quality predicate. | planned |
| `lean:efficiency.residual_record_required` | `AsiStackProofs.Efficiency` | Any compressed or routed result with unresolved obligations must carry a residual record. | planned |

### Failure Modes of Ungoverned Intelligence

Stable ID: `failure-modes-of-ungoverned-intelligence`

Chapter job: make the failure model concrete before proposing layer mechanisms.

Core claim: ungoverned intelligence fails through stack-level breakdowns before dramatic catastrophic scenarios are needed.

Draft arc:

- Define failure as boundary, authority, evidence, or memory breakdown.
- Map failures by layer: goal misbinding, authority creep, context pollution, evaluator drift, tool overreach, benchmark gaming, and residual hiding.
- Show how a harmless-seeming workflow can become unsafe when handoffs are implicit.
- Connect each failure mode to a later invariant or test.
- Establish the book's tone: serious risk modeling without hype.

Primary interfaces:

- Governance limits authority creep.
- VCM limits context pollution.
- Verification limits false certainty.
- Execution limits side effects.
- Evidence ratchets preserve regressions.

Primary source spine: `scf`, `vcm_public`, `talos`, `spinoza`, `field_of_god`, `viea`, `simulation_scaling`.

Draft deliverables:

- Layered failure taxonomy.
- Threat-to-invariant mapping.
- Scenario set for future Codex tests.

Lean proof targets:

| Tag | Lean module | Formal target | Status |
|---|---|---|---|
| `lean:failure.invariant_violation_blocks_promotion` | `AsiStackProofs.FailureModes` | A component with a failed required invariant cannot be promoted to a higher support or readiness state. | planned |
| `lean:failure.unbounded_authority_detected` | `AsiStackProofs.FailureModes` | Missing authority ceilings are represented as a detectable governance failure, not as an implicit permission. | planned |

## Part II - The Layers of the ASI Stack

Part job: specify each major layer as an engineering boundary. Each chapter should follow the same pattern: problem, insufficiency of current approaches, core claim, mechanism, interfaces, invariants, failure modes, minimal implementation, test plan, and source crosswalk.

### Alignment and Constitution

Stable ID: `alignment-and-constitution`

Chapter job: define the normative substrate that constrains goals, plans, and self-modification.

Core claim: the alignment layer should act as a constitutional substrate rather than a reactive refusal wrapper.

Draft arc:

- Separate moral commitments from implementation policies.
- Define constitutional constraints as planning and execution inputs.
- Treat dignity, agency preservation, corrigibility, and moral uncertainty as explicit requirements.
- Preserve philosophical or metaphysical source material as labeled interpretation, not ungrounded engineering proof.
- Define what a self-improvement proposal must not be allowed to optimize away.

Primary interfaces:

- Planning receives admissible-goal constraints.
- Governance receives authority and dignity constraints.
- Verification checks whether normative claims exceed evidence.

Primary source spine: `alignment_field`, `field_of_god`, `ethica_mechanica`, `eternal_code`, `coherence_exchange`.

Draft deliverables:

- Compact constitution.
- Value-conflict taxonomy.
- Self-modification ethics scenarios.

Lean proof targets:

| Tag | Lean module | Formal target | Status |
|---|---|---|---|
| `lean:alignment.constraint_filter_soundness` | `AsiStackProofs.Alignment` | A plan admitted by the alignment filter satisfies every active constitutional constraint predicate. | planned |
| `lean:alignment.self_modification_preserves_constitution` | `AsiStackProofs.Alignment` | An accepted self-modification cannot remove or weaken protected constitutional constraints. | planned |

### Governance and Stable Capability Fields

Stable ID: `governance-and-stable-capability-fields`

Chapter job: specify how components can improve without identity drift or authority expansion.

Core claim: governed self-improvement requires stable capability fields: stable semantic boundaries with replaceable implementations and bounded authority.

Draft arc:

- Define a stable capability field as identity, interface, authority ceiling, qualification evidence, and rollback policy.
- Explain the difference between replacing an implementation and changing the field's meaning.
- Define route validity and mutation constraints.
- Explain evaluator integrity and why a component cannot be the only judge of its replacement.
- Tie SCFs to recursive self-improvement.

Primary interfaces:

- Planning requests field capabilities.
- Routing selects qualified implementations.
- Execution invokes authorized routes.
- Evidence records qualification and regression results.

Primary source spine: `scf`, `talos`, `ladon_manhattan`, `viea`, `moecot`.

Draft deliverables:

- Stable capability field schema.
- Promotion/rollback lifecycle.
- Authority non-escalation proof target.

Lean proof targets:

| Tag | Lean module | Formal target | Status |
|---|---|---|---|
| `lean:scf.authority_non_escalation` | `AsiStackProofs.StableCapabilityFields` | Replacing an implementation inside a stable capability field cannot increase the field's authority ceiling. | planned |
| `lean:scf.replacement_preserves_field_identity` | `AsiStackProofs.StableCapabilityFields` | Qualified replacement preserves field identity when interface, semantics, and authority predicates remain satisfied. | planned |
| `lean:scf.rollback_after_failed_qualification` | `AsiStackProofs.StableCapabilityFields` | A failed qualification check leaves a rollback path to the previous qualified implementation. | planned |

### Planning and Control

Stable ID: `planning-and-control`

Chapter job: make planning its own control layer, not a hidden behavior inside memory, reasoning, or execution.

Core claim: planning converts goals into governed action without owning memory, reasoning, or side effects.

Draft arc:

- Define goal intake, task decomposition, dependency graphs, budgets, risk limits, and stop conditions.
- Distinguish strategic planning, tactical planning, scheduling, runtime replanning, and tool-choice justification.
- Define plan artifacts as inspectable contracts.
- Explain planner requests to VCM, routers, verifiers, and execution systems.
- Define scope-creep and authority-preservation failure tests.

Primary interfaces:

- Alignment filters goals.
- VCM supplies context packets.
- Routers select specialists.
- Execution runs typed jobs under authority limits.

Primary source spine: `planforge`, `planforge_compiler_arch`, `cognitive_compilation`, `viea`, `software_magic_grimoire`, `moecot`.

Draft deliverables:

- Plan artifact schema.
- Dependency and constraint examples.
- Replanning triggers and stop conditions.

Lean proof targets:

| Tag | Lean module | Formal target | Status |
|---|---|---|---|
| `lean:planning.decomposition_preserves_authority` | `AsiStackProofs.Planning` | Every subtask produced by decomposition has an authority ceiling no greater than the parent plan. | planned |
| `lean:planning.replanning_preserves_constraints` | `AsiStackProofs.Planning` | Runtime replanning preserves required constraints and stop conditions from the active plan. | planned |
| `lean:planning.dependencies_before_execution` | `AsiStackProofs.Planning` | A typed job cannot become executable until its declared dependencies are satisfied. | planned |

### Virtual Context Memory

Stable ID: `virtual-context-memory`

Chapter job: specify memory as governed context compilation, not transcript accumulation.

Core claim: VCM should compile active context packets from durable memory using evidence, authority labels, and planner requests.

Draft arc:

- Separate durable memory, retrieval, context selection, summary, and active context.
- Define context packets: source handles, provenance, authority labels, summaries, adequacy, and uncertainty.
- Explain context adequacy versus context admission.
- Define proof-carrying summary discipline.
- Show how privacy and behavioral authority labels survive summarization.

Primary interfaces:

- Planning requests context.
- Verification checks summary fidelity.
- Execution receives context appropriate to its authority.

Primary source spine: `vcm_public`, `vcm_editable`, `context_engineer`, `black_hole_context_manager`, `verification_bandwidth`, `moecot`, `viea`.

Draft deliverables:

- Context packet schema.
- Adequacy rubric.
- Summary fidelity and distractor-resistance tests.

Lean proof targets:

| Tag | Lean module | Formal target | Status |
|---|---|---|---|
| `lean:vcm.admission_not_adequacy` | `AsiStackProofs.VirtualContextMemory` | Context admission and context adequacy are separate predicates; admission alone does not imply adequacy. | planned |
| `lean:vcm.summary_preserves_authority_labels` | `AsiStackProofs.VirtualContextMemory` | A valid summary packet preserves every source authority label required by its inputs. | planned |
| `lean:vcm.execution_context_within_authority` | `AsiStackProofs.VirtualContextMemory` | Execution receives only context whose authority labels are permitted by the job authority ceiling. | planned |

### Labor and Execution OS

Stable ID: `labor-and-execution-os`

Chapter job: define how plans become typed jobs and artifacts without giving reasoning unrestricted side effects.

Core claim: execution should happen through a governed labor operating system with typed jobs, tool permissions, artifacts, audit logs, and replay.

Draft arc:

- Define typed job lifecycle: request, approval, run, artifact, verification, completion, rollback.
- Distinguish reasoning authority from action authority.
- Explain adapters, tool permissions, sandboxing, audit logs, and human approval gates.
- Show how repeated job patterns can become procedural memory.
- Connect execution records to evidence and source matrices.

Primary interfaces:

- Planning emits job contracts.
- Governance enforces permissions.
- VCM supplies authorized context.
- Verification evaluates produced artifacts.

Primary source spine: `talos`, `talos_md`, `viea`, `genesiscode`, `software_magic_grimoire`, `cognitive_loop_closure`, `moecot`, `spinoza_composer`, `ladon_manhattan`.

Draft deliverables:

- Typed job schema.
- Artifact graph lifecycle.
- Replay and audit requirements.

Lean proof targets:

| Tag | Lean module | Formal target | Status |
|---|---|---|---|
| `lean:talos.job_lifecycle_order` | `AsiStackProofs.ExecutionOS` | Typed job state transitions respect the required lifecycle order from request through completion or rollback. | planned |
| `lean:talos.execution_requires_permission` | `AsiStackProofs.ExecutionOS` | A job can execute a tool only when the tool permission is present in the active job contract. | planned |
| `lean:talos.audit_log_append_only` | `AsiStackProofs.ExecutionOS` | Audit records are append-only across valid execution transitions. | planned |

### Reasoning, Verification, and Epistemology

Stable ID: `reasoning-verification-and-epistemology`

Chapter job: specify how the stack knows what it knows, what it does not know, and when a claim changes state.

Core claim: reasoning and verification should maintain claim records with evidence tiers, contradiction handling, and calibrated uncertainty.

Draft arc:

- Define claim records, evidence tiers, contradiction links, belief revision, and uncertainty calibration.
- Explain why fluent generation is not verification.
- Define verification bandwidth and bounded-context limits.
- Distinguish argument, source-derived, prototype-backed, test-backed, empirical, and external-literature-backed states.
- Connect reasoning outputs to planning and governance decisions.

Primary interfaces:

- Planning receives verified constraints and open questions.
- Evidence ratchets update claim states.
- VCM supplies provenance-aware context.
- Execution produces artifacts to verify.

Primary source spine: `spinoza`, `uat`, `coherence_exchange`, `verification_bandwidth`, `treellm`, `viea`, `spinoza_composer`, `aletheia`.

Draft deliverables:

- Claim record schema.
- Contradiction-handling examples.
- Belief revision and uncertainty tests.

Lean proof targets:

| Tag | Lean module | Formal target | Status |
|---|---|---|---|
| `lean:evidence.no_self_promotion` | `AsiStackProofs.SupportStates` | A claim cannot promote itself from a support state to the same support state. | implemented |
| `lean:evidence.promotion_requires_witness` | `AsiStackProofs.SupportStates` | Promotion to a stronger support state requires an evidence witness of the required kind. | planned |
| `lean:evidence.contradiction_blocks_source_derived` | `AsiStackProofs.ClaimLedger` | An unresolved contradiction blocks promotion to `source-derived` or stronger support states. | planned |

### Routing and Modular Intelligence

Stable ID: `routing-and-modular-intelligence`

Chapter job: show how capability scales through bounded specialists instead of one universal runtime.

Core claim: ASI scales by routing across bounded specialist modules with explicit readiness gates and benchmark-driven promotion.

Draft arc:

- Define router, specialist, route contract, readiness gate, promotion, quarantine, and fallback.
- Explain minimum adequate route selection.
- Connect routing to stable capability fields.
- Define residual escrow: what the chosen route left unresolved.
- Explain benchmark pressure without benchmark worship.

Primary interfaces:

- Planning states capability requirements.
- Governance constrains available routes.
- Evidence ratchets decide promotion.
- Compression may supply smaller representations.

Primary source spine: `octopus_router`, `rmi`, `moecot`, `beastbrain`, `cognitive_loop_closure`, `benchmaxxing`, `moecot_md`, `rgs`.

Draft deliverables:

- Route selection lifecycle.
- Readiness gate table.
- Quarantine and fallback process.

Lean proof targets:

| Tag | Lean module | Formal target | Status |
|---|---|---|---|
| `lean:routing.route_validity_requires_qualification` | `AsiStackProofs.Routing` | A route is valid only if the selected specialist satisfies the requested capability qualification. | planned |
| `lean:routing.fallback_preserves_authority` | `AsiStackProofs.Routing` | Fallback routing cannot increase authority beyond the original route request. | planned |
| `lean:routing.quarantine_blocks_selection` | `AsiStackProofs.Routing` | A quarantined specialist cannot be selected by a valid route. | planned |

### Compression and Representation

Stable ID: `compression-and-representation`

Chapter job: make compression an accountable layer that improves capability without hiding unresolved complexity.

Core claim: compression is a first-class stack layer only when it exposes residual burden, fallback paths, and downstream utility tests.

Draft arc:

- Define compression as representation governance, not mere size reduction.
- Explain compact generative systems, low-rank/residual approaches, generate-verify-repair, semantic hierarchy, and edge efficiency as hypotheses to test.
- Define residual burden and downstream utility.
- Explain when compression should trigger probe-route fallback.
- Tie representation efficiency back to the Efficient ASI Hypothesis.

Primary interfaces:

- Routing consumes compressed specialists or representations.
- VCM uses summaries that must preserve authority labels.
- Evidence tests utility and residuals.

Primary source spine: `cgs`, `rgs`, `rankfold_neuralfold`, `rankfold_compressor`, `bbvca_v9`, `bbvca_main`, `bugbrain`, `simulation_scaling`, `treellm`.

Draft deliverables:

- Compression utility rubric.
- Residual burden ledger.
- Reconstruction and downstream utility tests.

Lean proof targets:

| Tag | Lean module | Formal target | Status |
|---|---|---|---|
| `lean:compression.residual_burden_required` | `AsiStackProofs.Compression` | A lossy compression with unmet reconstruction obligations must expose a residual burden record. | planned |
| `lean:compression.fallback_when_probe_fails` | `AsiStackProofs.Compression` | Failed utility probes trigger an explicit fallback route instead of silent acceptance. | planned |
| `lean:compression.utility_preorder` | `AsiStackProofs.Compression` | Compression utility can be modeled as a preorder over downstream task adequacy and cost. | planned |

### Mathematical and Search Substrates

Stable ID: `mathematical-and-search-substrates`

Chapter job: place alternative formal/search substrates inside the architecture without overclaiming them.

Core claim: alternative substrates belong in the stack as testable implementation hypotheses until evidence supports stronger claims.

Draft arc:

- Define why the stack can host multiple mathematical substrates.
- Explain formal calculi, semantic IR, temporal coils, geometric search, and executable specification as candidate mechanisms.
- Keep speculative mathematical claims separated from engineering invariants.
- Define what would count as evidence: proofs, benchmarks, integration tests, or implementation wins.
- Connect this chapter to Lean and protocol-schema appendices.

Primary interfaces:

- Planning may compile to alternative IRs.
- Verification may use proofs or symbolic checks.
- Routing may select substrate-specific specialists.

Primary source spine: `coilmoecot`, `temporal_coil_research`, `cognitive_compilation`, `genesiscode`, `viea`.

Draft deliverables:

- Substrate comparison table.
- Formalization candidates for Lean.
- Evidence threshold rubric.

Lean proof targets:

| Tag | Lean module | Formal target | Status |
|---|---|---|---|
| `lean:substrate.interface_refinement` | `AsiStackProofs.Substrates` | A substrate-specific implementation refines the required stack interface before it can be routed to. | planned |
| `lean:substrate.proof_artifact_scope` | `AsiStackProofs.Substrates` | A proof artifact promotes only the formal claim it actually states, not adjacent informal claims. | planned |

### Evidence, Benchmarks, and Ratchets

Stable ID: `evidence-benchmarks-and-ratchets`

Chapter job: define how claims, tests, residuals, regressions, and releases improve over time.

Core claim: the book should maintain evidence ratchets that connect claims, tests, residuals, regressions, and release history.

Draft arc:

- Define benchmark ratchets and why static benchmarks saturate.
- Explain residual preservation, hidden tests, regression generation, and anti-Goodhart checks.
- Define how source notes, tests, claims, schemas, and changelog entries interact.
- Connect evidence to readiness gates and release decisions.
- Explain how Codex-run validation fits without pretending unrun tests have results.

Primary interfaces:

- Every layer sends claims and artifacts into the evidence system.
- Governance uses evidence for promotion.
- Living methodology uses evidence for publication state.

Primary source spine: `benchmaxxing`, `moecot`, `rmi`, `cognitive_loop_closure`, `road_to_agi`, `viea`, `tokenmana`, `coherence_exchange`, `uat`.

Draft deliverables:

- Evidence ratchet lifecycle.
- Test-result record schema.
- Release gate checklist.

Lean proof targets:

| Tag | Lean module | Formal target | Status |
|---|---|---|---|
| `lean:ratchet.support_state_monotonic_with_evidence` | `AsiStackProofs.EvidenceRatchets` | Support-state promotion is monotonic only when the required evidence witness is present. | planned |
| `lean:ratchet.regression_preserves_failure_record` | `AsiStackProofs.EvidenceRatchets` | A regression event preserves its failure record in the evidence history. | planned |
| `lean:ratchet.no_result_without_run_record` | `AsiStackProofs.EvidenceRatchets` | A test result cannot be recorded without a corresponding run record. | planned |

## Part III - Integration and Implementation

Part job: assemble the layers into a reference architecture, then show how to build and maintain it incrementally.

### Integrated Reference Architecture

Stable ID: `integrated-reference-architecture`

Chapter job: walk through the whole machine from user intent to governed action and improvement.

Core claim: the stack can be specified as an integrated reference architecture with typed handoffs between layers.

Draft arc:

- Present the canonical end-to-end flow: intent, constitution, governance, planning, context, routing, reasoning, execution, evidence, update.
- Walk one scenario through every layer.
- Show where artifacts are created, checked, stored, and reused.
- Define the reference architecture's control planes and data planes.
- Identify minimal interfaces required for interoperability.

Primary source spine: `viea`, `moecot`, `scf`, `vcm_public`, `planforge`, `talos`, `spinoza`, `alignment_field`, `octopus_router`, `rmi`, `benchmaxxing`.

Draft deliverables:

- End-to-end sequence diagram.
- Interface inventory.
- Stack trace example.

Lean proof targets:

| Tag | Lean module | Formal target | Status |
|---|---|---|---|
| `lean:integration.end_to_end_authority_ceiling` | `AsiStackProofs.Integration` | In an end-to-end trace, no layer handoff exceeds the initial authorized ceiling without an explicit approval event. | planned |
| `lean:integration.trace_completeness` | `AsiStackProofs.Integration` | A complete stack trace includes intent, constraints, plan, context, route, job, artifact, and evidence records. | planned |

### Prototype Roadmap

Stable ID: `prototype-roadmap`

Chapter job: convert the reference architecture into a staged build plan.

Core claim: the prototype should grow in staged increments from artifact graph to governed capability replacement.

Draft arc:

- Define build stages: source inventory, artifact graph, context packets, typed jobs, claim ledger, route gates, benchmark ratchets, stable capability field replacement.
- For each stage, specify minimum implementation, validation, and rollback.
- Separate book prototypes, Lean proofs, data schemas, and executable system code.
- Identify what can be implemented now versus what remains research.
- Keep "done" tied to recorded evidence, not aspiration.

Primary source spine: `moecot`, `viea`, `road_to_agi`, `benchmaxxing`, `scf`, `vcm_public`, `planforge`, `beastbrain`, `moecot_md`, `coherence_exchange`, `beastbrain_timeless`.

Draft deliverables:

- Milestone roadmap.
- Prototype evaluation table.
- Open implementation backlog.

Lean proof targets:

| Tag | Lean module | Formal target | Status |
|---|---|---|---|
| `lean:roadmap.stage_prerequisites` | `AsiStackProofs.PrototypeRoadmap` | A prototype stage cannot start until its declared prerequisite stages are complete or explicitly waived. | planned |
| `lean:roadmap.no_replacement_before_scf` | `AsiStackProofs.PrototypeRoadmap` | Capability replacement cannot enter the roadmap before the stable capability field schema exists. | planned |

### Living Book Methodology

Stable ID: `living-book-methodology`

Chapter job: make the book's maintenance process an example of the architecture's evidence discipline.

Core claim: the living book should model stack discipline through Quarto source, inventories, evidence matrices, tests, changelogs, and releases.

Draft arc:

- Explain why this is a living technical book rather than a frozen manuscript.
- Define source ingestion, source notes, claim state transitions, chapter drafting, proof/code updates, and release validation.
- Show how a new AI paper enters the system and either updates a chapter, creates a chapter, or remains unassigned.
- Explain GitHub Pages, issue templates, and Codex skill usage.
- Define how readers can audit changes.

Primary source spine: `road_to_agi`, `benchmaxxing`, `viea`, `moecot`, `moecot_md`, `bugbrain`.

Draft deliverables:

- Living update protocol.
- New-paper triage checklist.
- Release checklist.

Lean proof targets:

| Tag | Lean module | Formal target | Status |
|---|---|---|---|
| `lean:method.source_note_required_for_source_derived` | `AsiStackProofs.LivingBook` | A claim cannot be marked `source-derived` unless a source note exists for the cited source. | planned |
| `lean:method.no_test_result_without_record` | `AsiStackProofs.LivingBook` | The book cannot report a test result unless a public-safe result record exists. | planned |
| `lean:method.changelog_required_for_support_change` | `AsiStackProofs.LivingBook` | A support-state change requires a changelog entry in the same release set. | planned |

## Bibliography and Source Corpus

Book location: Appendix G, `appendices/G_bibliography.qmd`

Bibliography job: distinguish the supplied ASI Stack source corpus from future external literature.

Structure:

- Primary ASI Stack source corpus generated from `sources/source_inventory.json`.
- Chapter assignment crosswalk generated from `book_structure.json`.
- External literature queue by area, with no fabricated citation metadata.
- Citation policy for source IDs, source notes, and evidence-state promotion.

Rules:

- Do not infer authors, dates, venues, or publication status from titles alone.
- Do not cite a source as support until it has been ingested and summarized in a source note.
- Use stable source IDs for current corpus references.
- Add conventional bibliography metadata only after it is verified.

## Candidate Expansion Slots

The current 16-chapter structure is sufficient for a first full-book drafting goal. These topics should not become new chapters unless drafting pressure proves they need their own boundary:

- Security and adversarial operations: currently distributed across Failure Modes, Governance, Execution OS, and Evidence Ratchets.
- Human interface and delegation: currently distributed across ASI Is a Stack, Alignment, Planning, and Integrated Reference Architecture.
- Deployment operations: currently handled by Prototype Roadmap and Living Book Methodology.
- External literature review: currently handled by Appendix G until it becomes large enough to need its own chapter or part.
- Formal proofs: currently handled by Mathematical and Search Substrates plus Appendices D/E and the `lean/` workspace.

## Drafting Order for the Full-Book Goal

1. Draft Part I first so every later chapter uses the same vocabulary.
2. Draft Part II in interface order: Alignment, Governance, Planning, VCM, Execution, Reasoning, Routing, Compression, Mathematical Substrates, Evidence.
3. Draft Part III after the layer chapters so the integrated architecture can reuse their exact interfaces.
4. Build source notes alongside chapters; do not promote claim support states before source notes exist.
5. Generate or validate `proofs/proof_manifest.json` from the proof tags in this outline before implementing Lean modules.
6. Add diagrams and schemas only when they clarify a boundary, invariant, proof target, or handoff.
7. Run validation and render after each structural or chapter batch.

## Definition of a Ready Full Draft

A first complete manuscript draft is ready when:

- Every chapter has prose in all standard sections.
- Every assigned source has either a source note or an explicit blocker.
- Every core claim appears in Appendix C with the correct support state.
- No test result is reported unless the test was implemented and run.
- Every `lean:*` proof target is either implemented with passing `lake build` output or has an explicit blocker.
- `proofs/proof_manifest.json` is current with the proof tags in this outline.
- Appendix G reflects the current source corpus and any verified external literature.
- The site renders cleanly from `main`.
