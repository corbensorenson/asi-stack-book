# Artifact compression request-to-closure refinement

The replacement model has eight reachable stages: artifact registration, encoding record, reconstruction verification, consumer probe, fallback preparation, use admission, observed consumption, and closure. It exposes 53 routes and separates artifact, consumer, use, policy, rights, codec, decoder, evidence, result, event, full-source, byte/residual, exact-replay, integrity, adversarial-mutation, task-probe, utility, rare-case, security, fallback, cost, outcome, descendant, support, and external-effect state.

`python3 scripts/validate_artifact_compression_refinement.py` independently reaches all 53 routes, rejects 44/44 non-accepting mutations, recompiles Lean, validates and digest-binds the exact 22-field compressed-artifact fixture, reruns the 3,936-byte RAW0 replay with a 4,434-byte archive and one rejected corrupt-byte mutation, reruns the three-observation NEURAL0 metadata import over one 100,000,000-byte decoded artifact, and preserves both no-change decisions.

The exact-use witness reaches closure; a failed task probe reaches executable-fallback preparation without gaining support or effect authority. These are finite local lifecycle facts and exact artifact identities, not codec correctness, NeuralFold reproduction, useful compression, semantic preservation, downstream utility, rare-case preservation, security, rights adequacy, cost benefit, deployed fallback/recovery, independent reproduction, transfer, or state of the art. Support-state and external-effect authority remain exactly `none`.
