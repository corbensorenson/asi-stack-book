# Curated Reader Prose Pass: Virtual Context ABI

Date: 2026-06-30

Chapter: `virtual-context-abi`

Reader manuscript file:
`editions/reader_manuscript/v1_0/chapters/virtual-context-abi.qmd`

Live source file: `chapters/virtual-context-abi.qmd`

Consolidation caveat:
`docs/chapter_consolidation_release_stability_review.md` records this chapter
inside a `deferred_for_release` package with
`semantic-pages-context-cells-and-certificates`. This pass curates the current
source chapter for the reader-manuscript cycle only. It does not execute,
reject, or authorize the future static context ABI merge, and it preserves the
current 54-chapter manifest boundary.

## Reader Promise

This pass should let a human reader understand why context has to be governed
before it becomes model-visible working memory.

The reader throughline is:

> context is not governed memory until it has an address, a version, a lease,
> an authority boundary, and a fault path.

## Curation Scope

- Reworked the generated reader baseline into a continuous chapter about VCM as
  the ABI between durable memory and model-visible working context.
- Centered the chapter on address, version, mount, snapshot, representation,
  admission, adequacy, lease, authority, receipt, and typed-fault discipline.
- Preserved the source boundary: VCM public v1, Context Engineer, Verification
  Bandwidth, VIEA, editable VCM, and MoECOT provide architecture vocabulary,
  context-supply-chain pressure, adequacy framing, and implementation-reference
  context, not local resolver evidence or runtime evidence.
- Preserved external context comparators as orientation only, including RAG,
  long-context position sensitivity, memory-operating-system work,
  long-context benchmarks, citation-support evaluation, adaptive retrieval, and
  prompt compression.
- Reworked the Mermaid diagram around task requests, semantic addresses,
  versions, mounts, snapshots, representation contracts, admission, adequacy,
  leased materializations, typed faults, and context receipts.
- Preserved the minimum viable implementation and beyond-state-of-the-art
  endpoint without implying a deployed VCM resolver, context compiler,
  snapshot service, adequacy classifier, VCM-Bench result, or MoECOT runtime.
- Preserved the proof/test boundary for `AsiStackProofs.VirtualContextABI`,
  `schemas/context_abi_record.schema.json`, and the synthetic
  context-admission/adequacy harness.

## Meaning-Preservation Checks

- The live chapter core claim remains a design rationale at `argument`
  support.
- The curated reader chapter does not change Appendix C, Appendix K, source
  mappings, proof status, test status, evidence level, or release records.
- The source mappings remain architectural support for stable addresses,
  versions, mounts, snapshots, representation contracts, admission/adequacy
  separation, authority labels, context receipts, typed faults, and
  runtime-reference context.
- `vcm_editable` remains connector/source-note context for terminology and
  future citation triage, not a local VCM-Bench result or model-facing result.
- `moecot` remains implementation-reference context only; runtime artifacts,
  logs, benchmark records, and context ABI behavior have not been imported or
  reproduced here.
- The current schema and synthetic harness validate record shape and
  cross-record consistency only; they do not prove resolver behavior, context
  compiler behavior, snapshot-service correctness, adequacy-classifier quality,
  transaction-store behavior, model-facing context quality, or benchmark
  performance.
- The current Lean module proves finite-record properties over snapshot-bound
  reference resolution and mandatory-miss fault behavior only; it does not
  implement a VCM resolver, materializer, compiler, or adequacy classifier.
- The consolidation caveat is preserved: this reader pass does not decide the
  future merge with `semantic-pages-context-cells-and-certificates`.

## Non-Claims

- This pass does not edit the live AI/research chapter.
- This pass does not change `book_structure.json`.
- This pass does not execute, reject, or authorize a chapter merge or fold.
- This pass does not alter Appendix C or Appendix K.
- This pass does not add, remove, normalize, or promote source records.
- This pass does not claim deployed VCM behavior, resolver correctness, context
  compiler correctness, snapshot-service behavior, adequacy-classifier quality,
  materialization correctness, summary fidelity, contradiction-rate reduction,
  distractor resistance, leak prevention, Digital SCIF behavior, transactional
  memory-store behavior, VCM-Bench performance, model-facing context quality,
  MoECOT runtime behavior, source-reported benchmark reproduction,
  source-interpretation adequacy, or support-state movement.
- This pass does not promote any chapter core claim above `argument`.
- This pass does not approve the curated reader manuscript or any EPUB, DOCX,
  PDF, HTML, audio, or audio-embedded EPUB artifact for release.

## Remaining Blockers

- Human editorial review has not approved this chapter for reader release.
- The future static context ABI consolidation package still needs a reviewer
  decision before any merge with
  `semantic-pages-context-cells-and-certificates`.
- Stronger VCM evidence still needs a public-safe resolver fixture set,
  context-compiler trace, adequacy-classifier test, snapshot/revocation
  conformance test, context-packet replay audit, contradiction-rate experiment,
  VCM-Bench reproduction, or runtime artifact that preserves admission,
  adequacy, authority, lease, fault, and residual boundaries.
- Any future support-state movement requires an accepted evidence-transition
  record; the current curated prose pass is reader work only.
