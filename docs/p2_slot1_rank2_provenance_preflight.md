# P2 Slot 1 Rank 2 Provenance Preflight

Date: 2026-07-17  
State: **passed before task content was opened**

The frozen replacement queue advances the Rust slot to
`typst__hayagriva-334`. Its public pull request is merged, the exact base commit
is the verified merge commit's parent, the Apache license blob is bound to that
base, and the benchmark image resolves to a Linux/AMD64 manifest digest. Raw API
and manifest receipts are SHA-256 bound in the machine record.

This provenance pass does not qualify the task. Its registry layers total about
1.56 GB, but that is not silently treated as the Docker Engine content size or
the virtual size. The exact digest must be pulled and measured under the frozen
v2 resource contract before any problem statement, patch, test command, oracle,
or outcome is opened. The final pool remains unselected and unopened.

Machine record: `evidence_quality/p2_slot1_rank2_provenance_preflight.json`.
