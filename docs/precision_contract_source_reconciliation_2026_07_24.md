# Precision Contract Source Reconciliation

Date: 2026-07-24
Decision: **integrate into existing owners; no new chapter now**
Support-state effect: **none**

## Intake result

The supplied Markdown and DOCX are retained as one Corben-authored source pair
under `sources/raw/corben_papers/precision_contract/`. The Markdown is the
canonical local text; the DOCX is the supplied presentation copy. The full
paper has been passage-reviewed. Its tracked public-safe source note is
`sources/source_notes/precision_contract.md`.

The paper makes a useful architectural move: it replaces universal per-weight
precision questions with a contract-relative search for the shortest complete
executable implementation that preserves protected behavior. Its distinctive
contribution is the join among representation invariance, complete
rate–distortion accounting, precision as a routed field, progressive residual
precision, transformation verification, and scoped certification.

## Owner decision

No new chapter is admitted. The current owners already partition the lifecycle:

| Owner | Required integration |
|---|---|
| `rankfold-neuralfold-and-artifact-compression` | Primary treatment of functional rate–distortion, behavioral equivalence classes, canonicalization, complete description accounting, the Functional Precision Compiler, and precision certificates. |
| `compact-generative-systems-and-residual-honesty` | Numeric precision as compact representation; base plus residual bit planes; contract benefit per added cost; reconstruction/behavior/utility/cost separation. |
| `fast-generation-architectures` | Static, layer-wise, mixed, progressive, and dynamic precision routing; safe operating envelope; escalation, abstention, and fallback. |
| `resource-economics-and-token-budgets` | Stored bits, moved bits, decoder work, latency, energy, verification, repair, and assurance-generation cost. |
| `readiness-gates-residual-escrow-and-quarantine` | `CERTIFIED`, `PROVISIONAL`, `DOMAIN_RESTRICTED`, `UNSAT`, `UNVERIFIED`, and `REVOKED` precision states; expiry, drift, revocation, and residual custody. |
| `executable-specifications-and-lean-proof-envelope` | Quantization as a program transformation; reference-to-bit-exact refinement; formal scope and evaluator limitations. |
| `model-weight-custody-and-hardware-roots-of-trust` | Derivative closure across quantized shards, codebooks, residuals, decoders, kernels, caches, and platform-bound hashes. |
| `the-efficient-asi-hypothesis` | Precision as one governed adaptive resource within route selection rather than a universal static property. |

The title `functional-precision-and-behavior-preserving-computation` is retained
only as a contingency label for a later owner audit. It is not a chapter
candidate in active research, a manifest reservation, or a target chapter
count. Reconsideration is allowed only after the coordinated integrations are
complete and only if the compiler/certificate lifecycle remains incoherent
under the present owners.

## Manuscript packet completed 2026-07-24

The coordinated prose transaction now:

1. adds one primary section-scale treatment to RankFold/NeuralFold;
2. adds bounded handoff sections or subsections to the seven supporting owners;
3. uses one shared `PrecisionContract` vocabulary and avoids eight independent
   restatements of the paper;
4. preserves protected-behavior, complete-accounting, and certificate-scope
   nonclaims at every handoff;
5. adds chapter-local failure treatment and one joined experiment program;
6. reconciles the live overview, glossary, Appendix C/G source surfaces, and
   Integrated Reference Architecture synthesis; and
7. changes no support state, release identity, or chapter count.

The prose acceptance is semantic rather than word-count based. A reader can
now follow:

`reference -> contract -> canonicalization -> allocation -> encoding ->
residual/routing -> verification -> certificate -> expiry/revocation`

without encountering duplicate ownership or an implied behavior-preservation
result. Schema, validator, formal, and empirical work remain separate evidence
residuals and are not implied by the completed manuscript transaction.

## External-source resolution

The paper cites 49 external works. They are not automatically added to Appendix
H. The source pass must first deduplicate them against the existing inventory,
verify primary publication records, and passage-review the exact result used.
The highest-priority families are:

- rate–distortion and quantization foundations;
- MDL and complete-description reasoning;
- floating-point and numerical-stability analysis;
- post-training, mixed, and progressive LLM quantization;
- formal verification and error bounds for quantized networks;
- quantization-specific security and router attack surfaces.

Already-inventoried items must be reused rather than duplicated. Recent or
provisional papers require current primary-source verification. External
source-reported results remain unreproduced unless an accepted local evidence
transition says otherwise.

## Roadmap placement

Source intake, storage, note, triage, owner adjudication, and the bounded
eight-owner manuscript integration are terminal. Corben's explicit request to
put all roadmap concepts into the live manuscript authorized the prose-only
transaction without opening evidence work or another chapter. The transaction
does not displace P2, activate a third empirical WIP lane, reopen structural
admission, or begin candidate N/O research. The remaining schema, validator,
formal, external-literature, and empirical work retains its ordinary roadmap
ordering and gates. In the roadmap's exact boundary language, this packet
**cannot displace P2**.

The later empirical program remains separately gated. It requires competent
strong baselines, complete physical-cost accounting, positive controls,
protected slices, independent-enough evaluation, and conservative routing
tests. A small broken quantizer or weak average-accuracy probe may debug the
pipeline but cannot falsify the architecture.

## Nonclaims

- No chapter is added, reserved, or promised.
- No quantizer, compiler, router, verifier, or certificate is implemented.
- No functional optimum or universal bit width is established.
- No source result becomes local empirical evidence.
- No support, readiness, release, deployment, publication, AGI, or ASI state
  changes.
