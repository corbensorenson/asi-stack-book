# Local Archive

This folder is for local-only context that should not be treated as canonical book source.

The ignored `_archive/local_context/` path may contain old handoff packets, conversation-mining drops, or previous scaffold attempts. Future drafting runs should use `book_structure.json`, `docs/book_outline.md`, tracked docs, tracked source notes, and validated appendices as the source of truth.
