# Benchmark Anti-Goodhart Harness Result

Date: 2026-06-28

Command:

```bash
python3 scripts/validate_benchmark_antigoodhart.py
```

Result:

```text
Benchmark anti-Goodhart harness passed: 2 valid fixture(s), 5 expected-invalid fixture(s).
```

Environment:

- Local repository checkout.
- Python 3.
- No network access required.
- No external packages beyond the repository's dependency-free schema subset
  validator.

Interpretation:

- The holdout-backed promotion and saturated-regression-floor fixtures passed.
- The saturated-promotion, missing anti-Goodhart checks, policy-from-blocked
  ratchet, reward-as-truth, and steward-release-without-approval fixtures were
  rejected as expected.
- No live-book support state changed.

Non-claims:

- This result does not prove benchmark quality, hidden-holdout integrity,
  contamination detection, transfer performance, policy-training quality,
  reward-hacking resistance, steward-agent behavior, release safety, or an AI
  safety property.
- This result does not promote any Appendix C claim.
