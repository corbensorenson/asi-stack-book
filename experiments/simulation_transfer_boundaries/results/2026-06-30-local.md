# Simulation Transfer Boundary Harness Result

Command:

```bash
python3 scripts/validate_simulation_transfer_boundaries.py
```

Local result:

```text
Simulation transfer boundary harness passed: 3 valid fixture(s), 6 expected-invalid fixture(s).
```

Boundary:

- The harness validates deterministic fixture discipline over
  `Simulation Contract Record` wrappers.
- It checks declared fidelity, resource bills, capacity bottlenecks, omitted
  variables, approximation liberties, instrumentation effects, transfer
  decisions, residual or downgrade behavior, and support-state non-promotion.
- It does not run a simulator, reproduce physical feasibility, validate
  open-world transfer, measure scheduler quality, prove economic optimality,
  or promote the Resource Economics chapter core claim.
