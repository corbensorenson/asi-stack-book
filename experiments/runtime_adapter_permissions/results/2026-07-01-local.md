# Runtime Adapter Permission Harness Result

Command:

```bash
python3 scripts/validate_runtime_adapter_permissions.py
```

Result:

```text
Runtime adapter permission harness passed: 2 valid fixture(s), 7 expected-invalid fixture(s).
```

## Fixture Delta

- Added `invalid_confused_deputy_ambient_authority.json`, which is otherwise
  schema-valid but attempts to execute a target-specific write by using the
  adapter's ambient authority instead of the caller's ceiling.
- Added `invalid_revoked_authority_receipt.json`, which is otherwise
  schema-valid but attempts to execute with an authority receipt marked
  revoked.

## Non-claims

- This is a synthetic record-gate result only.
- It does not execute a real adapter, prove sandbox isolation, prove approval
  service quality, prove confused-deputy resistance, prove revocation
  propagation in a deployed runtime, or prove secret-handle safety.
- It does not promote Runtime Adapters, System Boundaries, or any other chapter
  support state above `argument`.
