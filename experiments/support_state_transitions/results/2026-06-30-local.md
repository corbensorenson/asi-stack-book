# Support-State Transition Harness Result

Date: 2026-06-30

Command:

```bash
python3 scripts/validate_support_state_transitions.py
```

Result:

```text
Support-state transition harness passed: 4 valid fixture(s), 4 expected-invalid fixture(s).
```

Environment:

- Local repository checkout.
- Python 3.
- No network access required.
- No external packages beyond the repository's dependency-free schema subset
  validator.

Interpretation:

- The valid synthetic no-change, scoped upward-transition, downward
  scope-mismatch, and terminal-refutation fixtures passed.
- The invalid synthetic unreviewed-promotion, failed-verification-promotion,
  reasonless-downward-demotion, and unsupported-refutation fixtures were
  rejected as expected.
- Downward support movement now requires an accepted review, negative evidence
  refs, downgrade triggers, reviewer refs, and a `blocks_promotion` effect.
- Terminal `deprecated` or `refuted` transitions require accepted review,
  negative evidence refs, reviewer refs, and a `blocks_promotion` effect.
- No live-book support state changed.

Non-claims:

- This result does not promote, demote, deprecate, or refute any Appendix C
  claim.
- This result does not prove source interpretation, proof adequacy, benchmark
  performance, runtime behavior, reviewer independence, or an AI safety
  property.
