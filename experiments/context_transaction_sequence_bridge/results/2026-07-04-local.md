# Context Transaction Sequence Bridge - 2026-07-04 Local Result

Command:

```bash
python3 scripts/validate_context_transaction_sequence_bridge.py
```

Recorded result:

- `2 valid sequence fixture(s), 4 expected-invalid sequence fixture(s)` are present in `experiments/context_transaction_sequence_bridge/fixtures`.
- The valid fixtures cover bounded ordered transaction sequences for a linear write/read path and a tainted derivative path that remains blocked while undeclassified.
- The expected-invalid controls reject read-before-write ordering, missing replay boundary, undeclassified taint materialization, and support-state promotion attempts.
- The validator checks read-after-write discipline, artifact replay boundaries, taint blocking, invalid-control rejection, and support-state non-promotion for finite synthetic sequence fixtures.
- `AsiStackProofs.ContextTransactions` carries a finite sequence-summary bridge with the same fixture counts and non-promotion boundary.

Boundary:

- This is a synthetic finite sequence bridge.
- It does not implement a deployed transactional memory store.
- It does not prove runtime mount visibility, branch isolation, deletion closure, side-channel resistance, VCM conformance, context-manager benchmark behavior, or open-world poisoning resistance.
- It does not promote the chapter core claim or any Appendix C support state.
