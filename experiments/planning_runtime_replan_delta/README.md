# Planning Runtime-Replan Delta Audit

This directory holds the result artifact for the Planning Runtime-Replan Delta
Audit.

The validator is:

```bash
python3 scripts/validate_planning_runtime_replan_delta.py
```

The tracked local result is:

```text
experiments/planning_runtime_replan_delta/results/2026-07-02-local.json
```

This is a synthetic runtime-replan delta audit only. It is not a deployed
planner, runtime scheduler, route-quality, selected-tier-adequacy, live
feedback, evidence-transition, or support-state-promotion result.
