1. **Function Signature**: The function `receipt_matches` should accept two parameters: `claimed` (a string) and `observed` (a string).

2. **Type Checking**:
   - The function should check if both `claimed` and `observed` are strings.
   - It should also
