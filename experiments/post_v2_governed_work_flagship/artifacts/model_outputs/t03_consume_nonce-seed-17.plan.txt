To implement the `consume_nonce` function in Python, we need to ensure that the nonce is unique and not already present in the `used` set. Here's a step-by-step implementation:

1. **Check if the nonce is already in the `used` set**: If it is, return `(False, used
