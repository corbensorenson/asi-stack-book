```python
def total_visible_cost(items):
    """
    Calculate the sum of costs of items that are visible.

    Parameters:
    items (list of dict): A list of dictionaries, where each dictionary represents an item with a 'cost' and a 'visible' key.

    Returns:
    int: The sum of the costs of visible items.
    """
    total_cost = 0
    for item in items:
        if item['visible']:
            total_cost += item['cost']
    return total_cost

# Example usage:
items = [
    {'cost': 10, 'visible': True},
    {'cost
