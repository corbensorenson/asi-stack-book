To implement the `consume_nonce` function in Python, we need to ensure that the nonce is only accepted once and that the set of used nonces is updated accordingly. Here's a step-by-step implementation:

1. **Check if the nonce is already in the set**: If it is, return `(False, used
