```python
def consume_nonce(nonce, used):
    """
    Consumes a nonce and returns a tuple indicating whether the nonce was accepted and the new set of used nonce values.

    Parameters:
    nonce (str): The nonce to be consumed.
    used (set): A set of nonce values that have been used.

    Returns:
    tuple: A tuple containing a boolean indicating if the nonce was accepted and a set of used nonce values.
    """
    # Check if the nonce is already in the used set
    if nonce in used:
        return (False, used)
    
    # Add the nonce to the used
