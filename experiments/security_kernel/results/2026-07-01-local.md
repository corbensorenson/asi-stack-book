# Security Kernel Harness Result - 2026-07-01

Command:

```bash
python3 scripts/validate_security_kernel.py
```

Result:

```text
Security kernel harness passed: 3 valid fixture(s), 8 expected-invalid fixture(s).
```

Fixture delta:

- Added `invalid_expired_approval.json` to reject stale approval records that otherwise preserve the right schema shape.
- Added `invalid_overbroad_scif_context.json` to reject SCIF records that request broad context or private-source access instead of purpose-limited admission.
- Expanded the semantic validator's ambient-authority terms for overbroad context and private-source requests.

## Non-claims

This result is synthetic authority-use receipt discipline only. It does not prove deployed security-kernel behavior, sandbox isolation, secret-handle safety, side-channel resistance, runtime approval expiry enforcement, deployed least-privilege context behavior, prompt-injection containment, or security-overhead budget preservation.

## 2026-08-02 Lifecycle Revalidation

The original receipt fixtures remain unchanged. The validator now also
recompiles the exact 23-theorem authority-transaction lifecycle and independently
reconstructs eight accepted events plus nine rejecting controls.

```text
Security kernel harness passed: 3 valid fixture(s), 8 expected-invalid fixture(s). Security kernel formal binding passed: 2 retained authority-route theorem binding(s), 23 transaction-lifecycle theorem(s), 8 accepted event(s), and 9 rejecting lifecycle control(s).
```

This finite authored model does not establish complete runtime mediation,
secret non-disclosure, sanitizer or declassifier competence, effect-complete
revocation, isolation, side-channel control, prompt-injection containment,
privacy, security, support, or external effect.
