# Capacity Smoothing Fixtures

These fixtures support `python3 scripts/validate_capacity_smoothing.py`.

They are deterministic toy traces only. They do not run TokenMana, optimize a
production queue, prove load stability, measure human outcomes, or promote any
support state.
