# Generation Mode Baselines

This experiment folder contains deterministic v1.0 fixtures for the generation
mode baseline harness.

The harness checks accounting discipline for measured generation-mode records:
baseline references, quality/residual metrics, fallback behavior, resource
budget alignment, and non-promotion boundaries. It is not a model benchmark and
does not establish generation speed, model quality, or runtime behavior.
