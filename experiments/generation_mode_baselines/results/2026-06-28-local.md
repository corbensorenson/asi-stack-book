# Generation Mode Baseline Harness Result

Command:

```bash
python3 scripts/validate_generation_mode_baselines.py
```

Result:

```text
Generation mode baseline harness passed: 2 valid fixture(s), 4 expected-invalid fixture(s).
```

## What Ran

The local command validated deterministic JSON fixtures under
`experiments/generation_mode_baselines/fixtures/` against the existing
`generation_mode_record` and `resource_budget_record` schemas, then applied
semantic checks for baseline refs, quality/residual metric presence, fallback
behavior, resource-budget alignment, and no-promotion boundaries.

## Non-claims

- This is a deterministic fixture harness, not a model benchmark.
- This does not prove generation speed, speculative decoding quality, diffusion
  generation quality, KV-cache throughput, routing quality, or runtime behavior.
- This does not promote any Appendix C support state.
