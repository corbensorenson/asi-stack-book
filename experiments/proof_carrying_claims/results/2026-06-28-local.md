# Proof-Carrying Claim Harness Result

Date: 2026-06-28

Command:

```bash
python3 scripts/validate_proof_carrying_claims.py
```

Result:

```text
Proof-carrying claim harness passed: 3 valid fixture(s), 5 expected-invalid fixture(s).
```

Fixture scope:

- 3 valid fixtures cover a narrow formal pass, citation-dossier no-change
  treatment, and formalization mismatch escalation.
- 5 expected-invalid fixtures cover passed verifier results without artifact
  refs, mismatch promotion, formal tiers backed only by citations, missing
  failed-attempt refs, and timeout overpromotion.

Non-claims:

- This result does not prove theorem validity for arbitrary claims.
- This result does not prove semantic equivalence between prose and formal
  predicates.
- This result does not prove verifier quality, citation accuracy, runtime
  behavior, source interpretation, or support-state promotion.
