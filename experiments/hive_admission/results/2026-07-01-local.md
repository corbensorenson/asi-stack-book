# Hive Admission Harness Result

Command:

```bash
python3 scripts/validate_hive_admission.py
```

Observed output:

```text
Hive admission harness passed: 2 valid fixture(s), 8 expected-invalid fixture(s).
```

Scope:

- 2 valid synthetic hive admission scenarios.
- 8 expected-invalid controls for private data on rented nodes, high-risk jobs
  without bound approval, child/family-sensitive routing without guardian
  surface, external project work without a sandbox lease, energy over-budget
  selection, dropout without requeue residuals, missing audit replay refs, and
  support-state promotion overclaim.

Non-claims:

- No deployed personal compute hive was run.
- No scheduler, device registry, network overlay, approval service,
  guardian-policy engine, rented-node sandbox, or federation was exercised.
- No energy measurement, dropout recovery run, privacy guarantee, security
  result, benchmark result, source interpretation, or proof adequacy claim is
  created.
- No Appendix C support state or chapter core claim is promoted.
