# Plan-Execution Contract Harness Result

Date: 2026-07-02

Command:

```bash
python3 scripts/validate_plan_execution_contracts.py
```

Result:

```text
Plan-execution contract harness passed: 3 valid fixture(s), 10 expected-invalid fixture(s).
```

Environment:

- Local repository checkout.
- Python 3.
- No network access required.
- No external packages beyond the repository's dependency-free schema subset
  validator.

Interpretation:

- The valid linear dispatch, blocked-authority, and blocked
  authority-inference fixtures passed.
- The invalid cyclic-DAG, contract-mismatch, unreceipted-dispatch,
  requirement-loss, approval-bypass, unresolved-ambiguity dispatch,
  unrejected-hidden-override, authority-widening, inferred-field-dispatch, and
  inferred-authority-dispatch fixtures were rejected as expected.
- The field-confidence increment checks that dispatchable plans cannot depend
  on inferred or missing required command fields.
- The authority-inference increment checks that inferred authority can be
  recorded as a blocked residual/re-contract path, but cannot validate and
  dispatch a plan.
- No live-book support state changed.

Non-claims:

- This result does not prove planner quality, scheduler behavior, deployed
  execution, runtime adapter safety, semantic-parser quality, prompt-injection
  containment, authority-extraction quality, approval-service behavior,
  side-effect enforcement, benchmark performance, or an AI safety property.
- This result does not promote any Appendix C claim.
