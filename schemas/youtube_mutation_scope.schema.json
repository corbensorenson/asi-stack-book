{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://corbensorenson.github.io/asi-stack-book/schemas/youtube_mutation_scope.schema.json",
  "title": "ASI Stack YouTube Mutation Scope",
  "type": "object",
  "required": [
    "schema_version",
    "scope_id",
    "state",
    "channel_id",
    "channel_display_name",
    "upload_plan_path",
    "upload_plan_sha256",
    "chapter_count",
    "playlist_title",
    "allowed_platform_mutations",
    "allowed_repository_reconciliation",
    "prohibited_mutations",
    "stop_conditions",
    "approval_contract",
    "external_mutation_authorized_now",
    "support_state_effect",
    "book_claim_release_effect"
  ],
  "properties": {
    "schema_version": {"const": "asi_stack.youtube_mutation_scope.v1"},
    "scope_id": {"const": "p7.3-generation-1-complete-edition-publication"},
    "state": {"const": "scope_prepared_not_authorized"},
    "channel_id": {"const": "UCX7Tu67cGmKfT6O38xxiQFA"},
    "channel_display_name": {"const": "corben sorenson"},
    "upload_plan_path": {"const": "visual_edition/youtube_upload_plan.json"},
    "upload_plan_sha256": {"pattern": "^[0-9a-f]{64}$"},
    "chapter_count": {"type": "integer", "minimum": 0, "maximum": 1000},
    "playlist_title": {"const": "The ASI Stack — Visual Abstracts"},
    "allowed_platform_mutations": {
      "const": [
        "No platform mutation is in scope until an explicit local-artifact opt-in produces a non-empty upload plan"
      ]
    },
    "allowed_repository_reconciliation": {
      "const": [
        "record exact platform observation receipts",
        "reconcile channel playlist packet manifest ledger and managed Quarto embed state",
        "commit and push the receipt-bound repository state to main",
        "observe build deploy and public-crawl attestation"
      ]
    },
    "prohibited_mutations": {
      "const": [
        "delete or modify any pre-existing unrelated video playlist comment subscription or channel setting",
        "upload a master whose digest differs from the upload plan",
        "duplicate a completed upload when resuming after a limit or interruption",
        "publish a partial or unreconciled edition",
        "reuse this scope for a later generation supersession or any other project"
      ]
    },
    "stop_conditions": {
      "const": [
        "signed-in channel identity differs from the scoped channel",
        "a local input metadata caption or thumbnail digest differs",
        "YouTube reports a daily limit policy warning unresolved processing error or ambiguous mutation result",
        "playlist order metadata accessibility or public reachability cannot be verified exactly",
        "receipt or repository reconciliation validation fails"
      ]
    },
    "approval_contract": {
      "const": "Corben must explicitly authorize this exact scope at action time; preparing or hashing this file is not authorization."
    },
    "external_mutation_authorized_now": {"const": false},
    "support_state_effect": {"const": "none"},
    "book_claim_release_effect": {"const": "none"}
  },
  "additionalProperties": false
}
