---
title: "RankFold, NeuralFold, and Artifact Compression"
chapter_id: "rankfold-neuralfold-and-artifact-compression"
part_id: "routing-compression-representation-substrates"
status: "conceptual"
draft_maturity: "v0.2 manuscript draft"
last_updated: "2026-06-25"
primary_sources:
  - "rankfold_neuralfold"
  - "rankfold_compressor"
  - "bbvca_v9"
  - "cgs"
  - "bugbrain"
evidence_level: "argument"
claim_label: "Design rationale"
open_evidence_gaps:
  - "Appendix C now maps this artifact-compression claim to all assigned source notes; support remains argument until a compressor, deterministic decoder, corpus benchmark, or utility probe exists."
  - "The compressed_artifact_record fixture validates schema/example consistency only; no RankFold/NeuralFold codec, deterministic decoder, corpus benchmark, or utility probe has been run."
  - "No support-state promotion is implied by this drafting pass."
---

# RankFold, NeuralFold, and Artifact Compression

## Chapter status

| Field | Value |
|---|---|
| Chapter ID | `rankfold-neuralfold-and-artifact-compression` |
| Part | Part III - Routing, Compression, Representation, and Substrates |
| Status | conceptual |
| Manuscript maturity | v0.2 manuscript draft |
| Last updated | 2026-06-24 |
| Primary source records | `rankfold_neuralfold`, `rankfold_compressor`, `bbvca_v9`, `cgs`, `bugbrain` |
| Claim label | Design rationale |
| Evidence level | argument |
| Source queue | primary: `rankfold_neuralfold`, `rankfold_compressor`; supporting: `bbvca_v9`, `cgs`, `bugbrain` |
| Source loading state | source notes: `rankfold_neuralfold`, `rankfold_compressor`, `bbvca_v9`, `cgs`, `bugbrain`; raw cache: `rankfold_neuralfold`, `rankfold_compressor`, `bbvca_v9`, `cgs`, `bugbrain` |
| Test state | `compressed_artifact_record.valid.json` passes repository-level protocol fixture validation; `AsiStackProofs.ArtifactCompression` implements finite probe/fallback and residual/fallback metadata predicates; compression-ratio, behavioral probe-route fallback, and downstream-utility tests remain planned. |

## Drafting guardrail

This chapter uses RankFold/NeuralFold and RankFold Compressor as compression-architecture sources. It does not report compression ratios, lossless reconstruction, or implementation performance because this repository has not run a compressor, decoder, or benchmark corpus.

## Problem

The stack will store source documents, artifacts, logs, benchmark traces, models, simulations, and generated outputs. Storage and transfer cost matter, but artifact compression is dangerous when it damages semantics, loses decoder determinism, hides residual coding cost, or makes fallback so common that the apparent savings disappear.

RankFold/NeuralFold is useful here because it treats artifacts as candidates for tensor-centric archival structure while preserving manifest, codec, residual, and reconstruction obligations. BBVCA v9 adds the discipline that exactness requires a reconstruction contract and repair accounting.

## Why existing approaches are insufficient

Storage savings are not enough. A compressed artifact must be useful for the task that wants it. A low-rank transform can optimize reconstruction error while increasing encoded residual cost. A tensor representation can be elegant while losing exact file reconstruction. A write-once archive can amortize expensive encoding, but that assumption fails for hot artifacts that must be rewritten or queried constantly.

The book therefore treats artifact compression as a routed representation choice. A task can use a compressed artifact only after the relevant probe passes; otherwise it routes to the full artifact or records a residual.

## Core Claim

[rankfold-neuralfold-and-artifact-compression.core, label: Design rationale, support: argument] RankFold/NeuralFold-style artifact compression belongs as a bounded implementation hypothesis with probe-route fallback and residual coding.

The claim remains at `argument` support. The source notes support architecture discussion of low-rank residual transforms, artifact-to-tensor conversion, WORM archive assumptions, manifests, codec parameters, reconstruction checks, and deterministic decode obligations. They do not support measured compression-performance claims in this repo.

### Claim-source mapping status

Appendix C now maps this artifact-compression claim to all assigned source notes. The mappings support low-rank/tensor artifact compression, manifests, deterministic decode obligations, residual and fallback accounting, and resource-constrained lineage, not measured RankFold/NeuralFold performance.

| Source | What it supports | Limit |
|---|---|---|
| `rankfold_neuralfold` | Tensor-centric archival structure, per-artifact optimization, low-rank residual transforms, residual-coder cost alignment, WORM assumptions, manifests, codec parameters, deterministic decode, fuzz/integration tests, and reconstruction checks. | No local compression implementation, corpus benchmark, deterministic decoder, reproduced ratio, or utility probe exists here. |
| `rankfold_compressor` | Vector-pair and factorized representations, adaptive rank, normalization, entropy-aware coding, shape optimization, recursive cascades, correctness, determinism, metrics, and CLI plans. | Lineage source with broad claims; no compressor, ratio reproduction, lossless reconstruction validation, or public matrix corpus test has been run. |
| `bbvca_v9` | Reconstruction contracts, exact repairs, bounded verification, fallback, interface costs, and proxy/final-rate separation for artifact compression discipline. | Does not provide local RankFold performance evidence, artifact utility evidence, or codec correctness. |
| `cgs` | Compact seeds, generated structure, residual channels, verification contracts, governance interfaces, and hidden-complexity accounting. | Does not prove that any compressed artifact preserves downstream utility or cost advantage. |
| `bugbrain` | Tiny/local cognition, storage paging, embedded workflow, and edge-efficiency concerns that make artifact size and runtime burden visible. | Speculative prototype-lineage source; no embedded runtime, paging behavior, artifact compressor, or hardware benchmark has been validated. |

## Mechanism

The mechanism is a Compressed Artifact Record with explicit reconstruction, residual, probe, fallback, and utility fields.

```{mermaid}
flowchart LR
  A["Source artifact"] --> B["Compression method"]
  B --> C["Codec parameters + manifest"]
  C --> D["Residual coding"]
  D --> E["Decode / reconstruction check"]
  E --> F{"Task probe passes?"}
  F -- "yes" --> G["Use compressed artifact"]
  F -- "no" --> H["Route to full artifact"]
  G --> I["Cost, utility, and non-claim ledger"]
  H --> I
```

The record should distinguish four claims that are often collapsed:

- Representation claim: the artifact can be mapped into the chosen form.
- Reconstruction claim: decoding reproduces the required target under a contract.
- Compression claim: serialized size improves against a baseline after metadata and residuals.
- Utility claim: downstream tasks still work at acceptable quality and latency.

Each claim can fail separately. The architecture should preserve that separation instead of letting a good ratio imply task utility or a useful lossy representation imply exact reconstruction.

## Interfaces

The interface is the Compressed Artifact Record.

Minimum fields:

- `artifact_id`
- `source_artifact`
- `compression_method`
- `reconstruction_contract`
- `ratio_claim_state`
- `codec_parameters`
- `residual_coding`
- `probe_plan`
- `fallback_artifact`
- `decode_determinism`
- `utility_tests`
- `non_claims`

Artifact graphs store compressed and full references. Routing selects the representation by task and probe. Evidence records ratio, latency, residual size, decoder determinism, utility, and fallback frequency. Resource economics decides whether encoding cost is justified by the access pattern.

## Invariants

- Fallback path is known.
- Residual coding is measured.
- Downstream utility is tested.
- Compression ratios include metadata, parameters, seeds, manifests, and residuals.
- Lossless claims require a reconstruction contract and decode check.

The strongest invariant is decode accountability: if the system cannot say what exact target is reconstructed, under which arithmetic and serialization assumptions, then it cannot claim exact compression.

## Failure modes

- Compression damaging rare critical cases.
- Probe not representative.
- Latency gains erased by fallback.
- Encoder cost hidden behind write-once assumptions.
- Floating-point or dependency drift breaking deterministic decode.

Probe failure should route to the full artifact. Rare-case damage should become a regression. Hidden encoder cost should move into resource accounting. Decoder drift should block exactness and preserve the full artifact until deterministic replay is restored.

## Minimal implementation

The smallest useful implementation is a compressed artifact schema and fixture. The current fixture validates that a record names method, reconstruction contract, ratio claim state, codec parameters, residual coding, probe plan, fallback, decode determinism, utility tests, and non-claims.

Passing the fixture does not prove a compressor works. It proves the public book has an executable contract for evaluating one later.

## Codex test plan

| Test | Purpose | Status |
|---|---|---|
| Compressed artifact record fixture validation | Validate that an artifact-compression record names method, contract, ratio state, codec parameters, residuals, probes, fallback, decode determinism, utility tests, and non-claims. | implemented; passing via `python3 scripts/validate_protocol_examples.py` |
| Compression ratio test | Check that reported ratios include metadata, parameters, seeds, manifests, and residuals. | planned; not run |
| Probe-route fallback test | Check that a failed task probe routes to the full artifact. | planned; not run |
| Downstream utility preservation test | Compare compressed and full artifacts on a bounded task suite. | planned; not run |

The implemented row validates fixture/schema consistency only. The remaining rows require an actual compressor/decoder or a bounded toy surrogate, plus baseline artifacts and task probes; they are not reported results. When a behavioral test is implemented, the chapter should link to the command, fixture, environment notes, corpus, and result summary, and Appendix E should be regenerated or updated accordingly.

### Formalization hooks

| Tag | Module | Target | Status |
|---|---|---|---|
| `lean:compression.artifacts.operational_invariant` | `AsiStackProofs.ArtifactCompression` | A compressed artifact used for a task must pass that task probe or route to fallback. | implemented |
| `lean:compression.artifacts.failure_blocks_promotion` | `AsiStackProofs.ArtifactCompression` | A compression record cannot omit residual or fallback metadata. | implemented |

These Lean hooks are implemented as finite-record predicates over artifact-use and metadata fields. They prove only the record-level gates: a compressed artifact selected for a task must have a passed probe or fallback route, and a promoted compression record must expose residual and fallback metadata. They do not prove a compression ratio, decoder correctness, task utility, or benchmark result.

## Source crosswalk

| Source ID | Title | Layer | Planned use | Readiness |
|---|---|---|---|---|
| `rankfold_neuralfold` | RankFold + NeuralFold | compression_representation | Tensor/artifact compression. Low-rank residual coding plus functional preprocessing and probe-route fallback. | source note available; local raw cache available |
| `rankfold_compressor` | rankFold compressor | compression_representation | Alternate RankFold/NeuralFold source. | source note available; local raw cache available |
| `bbvca_v9` | BBVCA_v9_final_public_release | compression_representation | Prefer v9. Generate-verify-repair compression from seeded local laws, bounded search, two-phase rate discipline. | source note available; local raw cache available |
| `cgs` | Compact Generative Systems | compression_representation | Smallest adequate structure that can generate/govern target without hiding residual complexity. | source note available; local raw cache available |
| `bugbrain` | BugBrain | edge_efficiency_lineage | Edge/resource-aware implementation context. | source note available; local raw cache available |

The crosswalk keeps the implementation hypothesis bounded. `rankfold_neuralfold` supplies the mature artifact-archive framing, `rankfold_compressor` preserves low-rank/tensor lineage, `bbvca_v9` supplies reconstruction and rate-accounting discipline, `cgs` supplies residual honesty, and `bugbrain` keeps edge-resource constraints visible. The chapter should not use any of them to report ratios until a decoder and benchmark harness exist.

## Summary

Artifact compression is a routing decision under evidence. A compressed artifact may be cheaper to store, but it becomes usable only when reconstruction, residuals, probes, fallback, utility, and cost accounting are explicit.

RankFold/NeuralFold belongs in the stack as a bounded implementation hypothesis: promising enough to specify, not proven enough to promote without a benchmark harness and deterministic decode checks.
