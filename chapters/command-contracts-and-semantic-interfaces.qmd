---
title: "Command Contracts and Semantic Interfaces"
chapter_id: "command-contracts-and-semantic-interfaces"
part_id: "planning-memory-reasoning-execution"
status: "conceptual"
draft_maturity: "v0.2 manuscript draft"
last_updated: "2026-06-24"
primary_sources:
  - "software_magic_grimoire"
  - "viea"
  - "genesiscode"
  - "cognitive_compilation"
  - "talos"
evidence_level: "argument"
claim_label: "Design rationale"
open_evidence_gaps:
  - "Chapter is a v0.2 manuscript draft; v1.0 still needs source-note substantiation for all assigned sources."
  - "No Codex tests have been implemented or run for this chapter unless separately recorded in Appendix E."
  - "No support-state promotion is implied by this drafting pass."
---

# Command Contracts and Semantic Interfaces

## Chapter status

| Field | Value |
|---|---|
| Chapter ID | `command-contracts-and-semantic-interfaces` |
| Part | Part II - Planning, Memory, Reasoning, and Execution |
| Status | conceptual |
| Manuscript maturity | v0.2 manuscript draft |
| Last updated | 2026-06-24 |
| Primary source records | `software_magic_grimoire`, `viea`, `genesiscode`, `cognitive_compilation`, `talos` |
| Claim label | Design rationale |
| Evidence level | argument |
| Source queue | primary: `software_magic_grimoire`, `viea`; supporting: `genesiscode`, `cognitive_compilation`, `talos` |
| Source loading state | source notes: `software_magic_grimoire`, `viea`, `genesiscode`, `cognitive_compilation`, `talos`; raw cache: `software_magic_grimoire`, `viea`, `genesiscode`, `cognitive_compilation`, `talos` |
| Test state | Planned only; no tests have been run for this chapter in this drafting pass. |

## Drafting guardrail

This chapter is a v0.2 living-book draft. It synthesizes the architecture manifest, the current outline, the source inventory, available source notes, and local raw-source readiness. It does not by itself promote any claim beyond the support state shown in the chapter metadata and Appendix C. Where the chapter uses design reasoning, it is written as architecture argument rather than as reported empirical fact.

## Problem

The stack needs compact command language that is expressive enough for AI cooperation and precise enough for validation.

The previous chapter turns intent into a trace. This chapter defines the control language inside that trace. Software Magic Grimoire supplies the vocabulary: technical words compress mechanisms, responsibilities, workflows, failure shadows, and next actions. VIEA supplies the contract shape: role, objective, context, constraints, procedure, output, verification, and failure behavior.

A command contract is not a prettier prompt. It is the smallest artifact that lets a planner, verifier, tool runner, reviewer, or later agent understand what kind of work has been authorized and what would count as failure.

## Why existing approaches are insufficient

Prompt prose often hides objectives, constraints, output contracts, and failure behavior in unstructured language.

Free-form prompts are useful for exploration, but they are weak as operational boundaries. They mix discovery, design, implementation, verification, and recovery into one text blob. They also invite hidden instruction conflicts: a context fragment can smuggle a command that overrides the actual contract unless explicit fields dominate implicit prose.

GenesisCode adds a useful analogy from software: privileged effects and control responses need recognizable protocol shapes. In the ASI Stack, a command must expose output and verification requirements before it can be lowered into planning or execution.

## Core Claim

[command-contracts-and-semantic-interfaces.core, label: Design rationale, support: argument] Command contracts should use explicit semantic interfaces for objective, context, constraints, procedure, output, verification, and failure behavior.

The claim is architectural: it says how this layer should be shaped if the whole system is expected to remain governed under growth. The current support state is `argument`, so the claim should be read as a design rationale unless a later evidence bundle promotes it. The assigned source set - Software Magic Grimoire, Verified Intent-to-Execution Architecture, GenesisCode, Cognitive Compilation, and Talos Protocol - gives the chapter its vocabulary and comparison points, but the public manuscript keeps source-derived support separate from the draft itself.

## Mechanism

The mechanism is a Command Contract.

```{mermaid}
flowchart LR
  A["Natural-language request"] --> B["Semantic extraction"]
  B --> C["Role + objective"]
  C --> D["Context + constraints"]
  D --> E["Procedure + allowed means"]
  E --> F["Output contract"]
  F --> G["Verification contract"]
  G --> H{"Failure behavior declared?"}
  H -- "yes" --> I["Planning input"]
  H -- "no" --> J["Missing-contract residual"]
```

- Turn recurring command patterns into reusable templates.
- Treat software words as coordination primitives.
- Bind semantic interfaces to schemas and tests.

The command contract should be compact enough to write often and typed enough to validate. Its field names matter because they become routing handles. "Verification" routes to evidence and tests. "Failure behavior" routes to residuals and escalation. "Allowed means" routes to authority. "Output contract" routes to artifact checks.

Promptcraft remains useful, but only inside this envelope. The command can include a procedure or style, yet those instructions cannot override explicit constraints, authority ceilings, source boundaries, or required verification.

## Interfaces

The interface is the Command Contract.

- Planning consumes structured commands.
- Execution enforces output contracts.
- Verification checks stated failure behavior.

Minimum fields:

- `contract_id`
- `intent_id`
- `role`
- `objective`
- `context_refs`
- `constraints`
- `procedure`
- `allowed_means`
- `forbidden_means`
- `output_contract`
- `verification`
- `failure_behavior`
- `authority_ceiling`
- `required_approvals`
- `expected_artifacts`
- `feedback_route`

Planning consumes the structured command. Execution enforces allowed means and output contracts. Verification checks the declared criteria and failure behavior. Evidence records which fields were satisfied, repaired, escalated, or left residual.

## Invariants

The invariants are the conditions that must remain true even when the implementation changes, the source set grows, or later chapters introduce stronger mechanisms.

- A command exposes output and verification requirements.
- Failure behavior is declared.
- Implicit instructions do not override explicit constraints.

The override rule is central: context can inform a command, but it cannot silently replace explicit constraints. If a source, tool, user message, or retrieved note conflicts with the command contract, the system should emit a conflict residual instead of choosing the most fluent interpretation.

## Failure modes

The failure model is part of the design, not an afterthought. This layer is expected to fail in ways that can be detected, quarantined, and used to improve the architecture.

- Semantic ambiguity.
- Prompt injection through context.
- Unspecified output contract.

Semantic ambiguity produces false agreement: everyone thinks the command is clear until the artifact arrives. Prompt injection through context attacks the contract boundary. An unspecified output contract makes verification subjective. All three should become missing-contract or conflict records before planning proceeds.

## Minimal implementation

A canonical command-contract template used by future chapter drafting and experiment prompts.

The smallest useful implementation is a `command_contract` schema and fixture. The fixture should be public-safe and should demonstrate constraints, output, verification, approvals, and failure behavior without claiming the command has been executed.

The implementation should be promoted only when its evidence exists. A planned test remains planned; a proof target remains planned; a prototype claim remains unpromoted until the code, command, environment, and result are recorded.

## Codex test plan

| Test | Purpose | Status |
|---|---|---|
| Command schema validation test | Check that every command exposes objective, constraints, output contract, verification, and failure behavior. | planned; not run |
| Failure-behavior declaration test | Check that missing or vague failure behavior blocks planning dispatch. | planned; not run |
| Prompt override scenario | Check that hidden or conflicting context instructions cannot override explicit constraints. | planned; not run |

These tests are acceptance targets for later implementation work. They are not reported results. When a test is implemented, the chapter should link to the command, fixture, environment notes, and result summary, and Appendix E should be regenerated or updated accordingly.

### Formalization hooks

| Tag | Module | Target | Status |
|---|---|---|---|
| `lean:command.semantic_interface.operational_invariant` | `AsiStackProofs.CommandContracts` | A valid command contract contains objective, constraints, output contract, verification, and failure behavior. | planned |
| `lean:command.semantic_interface.failure_blocks_promotion` | `AsiStackProofs.CommandContracts` | A hidden or conflicting instruction cannot override an explicit contract constraint. | planned |

The table records proof and contract hooks for future work. Planned hooks should become Lean proofs only when the predicate is precise enough to mechanize; otherwise they should become schemas, process contracts, or research-agenda items.

## Source crosswalk

| Source ID | Title | Layer | Planned use | Readiness |
|---|---|---|---|---|
| `software_magic_grimoire` | Software Magic Grimoire | command_contracts_promptcraft | Structured command contracts and semantic density of software words. | source note available; local raw cache available |
| `viea` | Verified Intent-to-Execution Architecture | whole_stack_execution_spine | Keystone source. Human intent -> command contracts -> artifacts -> routing -> runtime targets -> verification -> deployment -> feedback. | source note available; local raw cache available |
| `genesiscode` | GenesisCode | executable_specification | Tiny pure calculus + obligations + provenance for auditable AI-symbiotic programming. | source note available; local raw cache available |
| `cognitive_compilation` | Cognitive Compilation | planning_semantic_ir | Compiler framing for LLM-centered planning, semantic IR, target compilation, incremental repair. | source note available; local raw cache available |
| `talos` | Talos Protocol | labor_execution_os | AI labor OS. Deterministic cognitive manufacturing, typed jobs, control planes, auditability, tool isolation. | source note available; local raw cache available |

The crosswalk describes why each source is assigned to the chapter and what is currently available in the repo. A local raw cache or source note makes future mining easier, but this draft still keeps the support state conservative until the specific claim-to-source mapping is recorded.

## Summary

Command Contracts and Semantic Interfaces gives the stack an operational language. It compresses intent into fields that planners, tools, validators, and reviewers can all read without inheriting hidden authority.

The chapter's discipline is explicitness under compression: make commands short enough to use, but structured enough that failure, verification, and authority remain visible.
