---
title: "Context Transactions, Snapshots, Mounts, and Taint"
chapter_id: "context-transactions-snapshots-mounts-and-taint"
part_id: "planning-memory-reasoning-execution"
status: "conceptual"
draft_maturity: "v0.2 manuscript draft"
last_updated: "2026-06-24"
primary_sources:
  - "vcm_public"
  - "ladon_manhattan"
  - "context_engineer"
  - "black_hole_context_manager"
  - "vcm_editable"
evidence_level: "argument"
claim_label: "Design rationale"
open_evidence_gaps:
  - "Chapter is a v0.2 manuscript draft; v1.0 still needs source-note substantiation for all assigned sources."
  - "No Codex tests have been implemented or run for this chapter unless separately recorded in Appendix E."
  - "No support-state promotion is implied by this drafting pass."
---

# Context Transactions, Snapshots, Mounts, and Taint

## Chapter status

| Field | Value |
|---|---|
| Chapter ID | `context-transactions-snapshots-mounts-and-taint` |
| Part | Part II - Planning, Memory, Reasoning, and Execution |
| Status | conceptual |
| Manuscript maturity | v0.2 manuscript draft |
| Last updated | 2026-06-24 |
| Primary source records | `vcm_public`, `ladon_manhattan`, `context_engineer`, `black_hole_context_manager`, `vcm_editable` |
| Claim label | Design rationale |
| Evidence level | argument |
| Source queue | primary: `vcm_public`; supporting: `ladon_manhattan`, `context_engineer`, `black_hole_context_manager`; connector/recovery: `vcm_editable` |
| Source loading state | source notes: `vcm_public`, `ladon_manhattan`, `context_engineer`, `black_hole_context_manager`, `vcm_editable`; raw cache: `vcm_public`, `ladon_manhattan`, `context_engineer`, `black_hole_context_manager`; connector/recovery: `vcm_editable` |
| Test state | Planned only; no tests have been run for this chapter in this drafting pass. |

## Drafting guardrail

This chapter is a v0.2 living-book draft. It synthesizes the architecture manifest, the current outline, the source inventory, available source notes, and local raw-source readiness. It does not by itself promote any claim beyond the support state shown in the chapter metadata and Appendix C. Where the chapter uses design reasoning, it is written as architecture argument rather than as reported empirical fact.

## Problem

Memory and context need runtime semantics for consistency, branches, contradiction, deletion, privacy, and taint.

The previous chapter gave context representations certificates. This chapter gives the memory system runtime semantics. Long-horizon work needs snapshots that are coherent enough to replay, mounts that define what memory is visible, branches that support experiments, taint that follows sensitive or suspect sources, and deletion closure that prevents derived summaries from resurrecting removed data.

VCM supplies the transaction model. Context Engineer and Ladon add need-to-know and sensitive-mount pressure. Black Hole Context Manager adds practical memory-budget machinery: chunks, drift, mass, freezing, eviction, and critical context preservation.

## Why existing approaches are insufficient

A retrieval store cannot guarantee read-your-writes behavior, copy-on-write experimentation, deletion closure, or taint propagation by itself.

Retrieval can return plausible neighbors, but it does not by itself define consistency. A worker may read a mixture of old and new facts, see a source after revocation, miss a contradiction, or consume a summary derived from deleted data. Security labels can also evaporate when context is copied, summarized, embedded, logged, or sent to a tool.

The stack needs database-like discipline without pretending memory is a database table. Context objects are semantic, versioned, compressed, mounted, and task-relative. That makes transaction semantics more important, not less.

## Core Claim

[context-transactions-snapshots-mounts-and-taint.core, label: Design rationale, support: argument] VCM should use transactional memory semantics: immutable events, versioned pages, snapshots, mounts, taint, temporal validity, and deletion closure.

The claim is architectural: it says how this layer should be shaped if the whole system is expected to remain governed under growth. The current support state is `argument`, so the claim should be read as a design rationale unless a later evidence bundle promotes it. The assigned source set - Virtual_Context_Memory_v1, Ladon & The Manhattan Protocol, Context Engineer / Manhattan Protocol, Black Hole Context Manager, and Virtual_Context_Memory_v1.0_Editable - gives the chapter its vocabulary and comparison points, but the public manuscript keeps source-derived support separate from the draft itself.

## Mechanism

The mechanism is a Context Transaction Record.

```{mermaid}
flowchart LR
  A["Immutable memory event"] --> B["Versioned page"]
  B --> C["Mount policy"]
  C --> D["Snapshot"]
  D --> E["Read / write / derive"]
  E --> F{"Branch or commit?"}
  F -- "branch" --> G["Copy-on-write branch"]
  F -- "commit" --> H["Committed event"]
  E --> I["Taint + deletion closure"]
  G --> I
  H --> I
  I --> J{"Closure satisfied?"}
  J -- "yes" --> K["Materializable view"]
  J -- "no" --> L["Fault / quarantine"]
```

- Record immutable memory events.
- Compile task snapshots with mounts and copy-on-write branches.
- Propagate taint and deletion obligations through derived context.

Immutable events make audit and replay possible. Versioned pages make supersession explicit. Snapshots give a task a coherent read view. Mounts define what can be seen and written. Copy-on-write branches let the system test alternate summaries, repairs, or plans without mutating the committed memory space.

Taint should propagate through derived cells unless authorized declassification occurs. Deletion should close over derivatives or fault when closure cannot be proven. Contradictions should become evidence records, not overwritten memory.

## Interfaces

The interface is the Context Transaction Record.

- Planning receives consistent views.
- Security labels sensitive mounts.
- Evidence records contradictions and supersession.

Minimum fields:

- `transaction_id`
- `operation`
- `snapshot_id`
- `mounts`
- `read_set`
- `write_set`
- `branch_policy`
- `taint_labels`
- `deletion_obligations`
- `contradiction_refs`
- `closure_state`
- `faults`
- `audit_refs`

Planning receives consistent views. Security labels sensitive mounts and tainted derivatives. Evidence records contradictions, supersession, invalidation, deletion closure, and faults.

## Invariants

The invariants are the conditions that must remain true even when the implementation changes, the source set grows, or later chapters introduce stronger mechanisms.

- Snapshots are consistent.
- Taint propagates to derivatives.
- Deletion closure is enforced or faulted.

The deletion invariant is strict: deleted or revoked source material cannot reappear through derived summaries unless a policy explicitly says what remains permitted. If closure cannot be established, the system should fault rather than materialize the view.

## Failure modes

The failure model is part of the design, not an afterthought. This layer is expected to fail in ways that can be detected, quarantined, and used to improve the architecture.

- Memory poisoning.
- Stale or contradictory reads.
- Deleted data reappearing in derived summaries.

Memory poisoning enters through malicious, stale, or over-weighted chunks. Stale or contradictory reads produce incoherent plans. Deleted data reappearing in summaries creates legal, privacy, and trust failures. These are transaction faults, not mere retrieval imperfections.

## Minimal implementation

A transaction model for context reads, writes, branches, and deletion closure.

The smallest useful implementation is a `context_transaction_record` schema and fixture. It validates operation, snapshot, mounts, read/write sets, branch policy, taint labels, deletion obligations, contradiction refs, closure state, faults, and audit refs. It does not implement a memory store.

The implementation should be promoted only when its evidence exists. A planned test remains planned; a proof target remains planned; a prototype claim remains unpromoted until the code, command, environment, and result are recorded.

## Codex test plan

| Test | Purpose | Status |
|---|---|---|
| Read-your-writes snapshot test | Check that a snapshot read sees committed events in its declared view. | planned; not run |
| Taint propagation test | Check that tainted source context produces tainted derived context unless declassification is authorized. | planned; not run |
| Deletion closure test | Check that deleted or revoked source material cannot reappear through derivatives without a closure record. | planned; not run |

These tests are acceptance targets for later implementation work. They are not reported results. When a test is implemented, the chapter should link to the command, fixture, environment notes, and result summary, and Appendix E should be regenerated or updated accordingly.

### Formalization hooks

| Tag | Module | Target | Status |
|---|---|---|---|
| `lean:vcm.transactions.operational_invariant` | `AsiStackProofs.ContextTransactions` | A snapshot read sees the committed events in its declared view. | planned |
| `lean:vcm.transactions.failure_blocks_promotion` | `AsiStackProofs.ContextTransactions` | A tainted source produces tainted derived context unless declassification is authorized. | planned |

The table records proof and contract hooks for future work. Planned hooks should become Lean proofs only when the predicate is precise enough to mechanize; otherwise they should become schemas, process contracts, or research-agenda items.

## Source crosswalk

| Source ID | Title | Layer | Planned use | Readiness |
|---|---|---|---|---|
| `vcm_public` | Virtual_Context_Memory_v1 | memory_context | Public VCM release. Governed protocol for compiled working context. | source note available; local raw cache available |
| `ladon_manhattan` | Ladon & The Manhattan Protocol | security_governance | Kernel-level security architecture for high-agency AI. | source note available; local raw cache available |
| `context_engineer` | Context Engineer / Manhattan Protocol | memory_context_lineage | Context as supply chain; compartmentalized information logistics. | source note available; local raw cache available |
| `black_hole_context_manager` | Black Hole Context Manager | memory_context_lineage | Earlier context management spec with lazy evaluation, batching, drift checks, security hardening. | source note available; local raw cache available |
| `vcm_editable` | Virtual_Context_Memory_v1.0_Editable | memory_context | Editable version with evidence-carrying planner-guided context compiler framing. | source note available; connector or recovery required |

The crosswalk describes why each source is assigned to the chapter and what is currently available in the repo. A local raw cache or source note makes future mining easier, but this draft still keeps the support state conservative until the specific claim-to-source mapping is recorded.

## Summary

Context Transactions, Snapshots, Mounts, and Taint gives VCM runtime semantics. It treats memory as immutable events, versioned pages, coherent snapshots, mounted views, copy-on-write branches, tainted derivatives, deletion obligations, contradiction records, and auditable faults.

The rule is that memory operations must leave a reviewable trace. If a context view cannot satisfy consistency, taint, or deletion closure, it should fault before becoming working context.
